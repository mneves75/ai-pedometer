# Coding Style and Naming

## Swift Language
- Swift 6.2 with strict concurrency; warnings are treated as errors via `Config/*.xcconfig`.

## Formatting

## Localization
- Add strings to `Shared/Resources/Localizable.xcstrings` with clear comments.
