# 2026-09-11

- Continuacao do passe de 09-10. Entregue `f3a077f` como 1.0 (56) e `08914ab` como correcao de CI, ambos em `origin/master`. CI 34565873880 e CodeQL 34565873872 passaram em `08914ab`. Primeira versao numerada para entrega na App Store; **nao e enviavel** (ver bloqueio abaixo).
- E2E completo de simulador rodou verde pela primeira vez desde o conserto do seletor de toolchain: unit 607/607, UI 20/20, zero falhas, zero skips, ambos os result bundles validados, screenshots capturados, saida 0. Antes disso `Scripts/e2e-simulator.sh` morria na primeira linha. Diretorio: `output/e2e-2026-09-11-015543/`.
- Tese de melhoria vinda de pesquisa externa, nao de gosto: analise do GitHub sobre 2.500+ repositorios e um estudo da ETH Zurich indicam que arquivos de instrucao de agente frequentemente *pioram* o resultado quando duplicam o que ja esta no repo, e que regra em prosa alcanca ~25-40% de aderencia contra ~95% quando virá portao executavel. Por isso o foco desta rodada foi mover invariantes de documentacao para checagem.
- Criado `Scripts/verify-swift-build-settings.sh`: falha quando `project.yml` declara um `SWIFT_*` que o Xcode selecionado nao define, conferido contra `Swift.xcspec`. Escopo deliberadamente estreito na familia `SWIFT_*`, que tem uma fonte autoritativa unica; alargar sem fonte equivalente produziria falso positivo, pior que portao nenhum. O teste planta um nome fabricado para provar que dispara e usa um setting real-mas-nao-usado como controle limpo. `preflight.sh` virou passo inicial do CI e `package.json` expoe `pnpm check`/`check:full`.
- **Quebrei o CI e consertei.** `f3a077f` falhou com `mapfile: command not found`. macOS entrega bash 3.2 em `/bin/bash` e os runners macOS do GitHub usam ele; meu shell local e bash 5.3, entao passou aqui e morreu la. Substituido por um loop `read`. O portao se comportou corretamente enquanto quebrado — reportou `extracao quebrada` e saiu diferente de zero em vez de passar vazio. O teste agora roda o script sob `/bin/bash` explicitamente, nos dois casos (passa e viola), para que um builtin de bash 4+ nao chegue mais ao verde.
- Evidencia incidental do run vermelho: o runner selecionou **Xcode 26.3** enquanto este host esta em 27.0. A mudanca para faixa suportada de 09-10 e o que faz os dois funcionarem; o pin duro em 26 era a unica coisa mantendo o CI vivo enquanto todo script local ja estava morto.
- Revisao das mudancas de 09-10 contra a documentacao Apple empacotada no Xcode 27 e o SDK do iOS 27: `Bundle` carrega `NS_SWIFT_SENDABLE` em `NSBundle.h`, entao cachear em `static let` e legal e nao um escape `@unchecked`; `.task` sem id para carga unica bate com a orientacao SwiftUI atual e o guard `importedRoute == nil` e idempotente porque a remocao limpa o storage antes do estado; nenhum build setting removido era relevante a seguranca. Revisao de seguranca do diff: sem regressao, o portao de override em Release continua valido.
- Instalado e rodando em iMarcus (iPhone 17 Pro Max fisico): AI Pedometer 1.0 (56), confirmado por `devicectl` e nao pela saida do instalador, com dois processos vivos — o app e a extensao de widget, o que tambem confirma o embed. Build Debug com Xcode 27.0 a partir de arvore limpa em `08914ab`. O launch funcionou; na tentativa anterior o telefone estava bloqueado e caiu no retry `Locked` ja documentado.
- **Bloqueio duro de producao, inalterado:** `Config/Local.xcconfig` carrega chave Test Store (`test_`) do RevenueCat. `AppConstants.RevenueCat.resolveConfiguration:117` corretamente anula chave `test_` fora de DEBUG, entao um archive Release resolve chave **nenhuma** e premium, compra e restore ficam mortos. A chave e assada no `Info.plist` em tempo de archive, entao configurar uma chave Apple (`appl_`) depois exige archive novo, nao edicao de config. Por isso o app funciona ponta a ponta no build Debug de iMarcus de um jeito que nao funcionaria em Release.
- Autenticacao ASC esta saudavel: `asc auth doctor` reporta sete checagens OK, perfil `AIPedometer` completo no keychain e como default, chave privada valida. Ressalva: isso prova que a chave carrega e e parseavel, nao que tem papel de publicacao — isso so aparece numa tentativa real de upload.
- Nenhum archive, IPA, tag ou deploy foi produzido, e autoreview seguiu sem rodar (nenhuma engine produziu saida). A revisao independente veio de tres subagentes de contexto fresco. Duas das descobertas deles estavam erradas e foram corrigidas por verificacao: `asc auth doctor` existe (o agente primeiro disse que nao, depois se corrigiu sozinho) e a documentacao que ele queria "consertar" ja estava certa.
