# 2026-06-10 (sessão iniciada 2026-06-09)

## Terceiro `/goal` full review cycle → 0.87 (43)

Security + arquitetura + performance + testes (4 agentes) com verificação adversarial,
`audit-xcode-security-settings`, simplify (4 agentes), fixes, bump, release.

### CRITICAL — ambiente de toolchain mudou nesta máquina

- `xcode-select` agora aponta para **Xcode 27.0 beta (27A5194q, Swift 6.4)**; o estável
  26.6 (17F109) está em `/Applications/Xcode.app`. TODOS os builds/testes do repo devem
  usar `DEVELOPER_DIR=/Applications/Xcode.app` (contrato do projeto = Xcode 26.x).
- O RevenueCat pinado (`89c9d14`) **não compila em test builds sob Swift 6.4**
  (`PaywallColor.swift: invalid redeclaration of synthesized memberwise init`). Build
  normal passa; `xcodebuild test` falha. Não é bug do repo; ressurgirá se o default
  continuar no beta. Saídas futuras: pinar revisão mais nova do RevenueCat OU manter
  DEVELOPER_DIR no 26.x.
- Xcode 26.6 inicialmente via ZERO destinos de simulador: o SDK dele é iOS 26.5.1
  (23F81a) e o runtime instalado é 23F73 → "iOS 26.5 is not installed". FIX:
  `xcrun simctl runtime match set iphoneos26.5 23F73` (com DEVELOPER_DIR apontando
  para o 26.6). É a lição de build-version drift do MEMORY.md, agora no iOS.
- Seletores de teste Swift Testing via `-only-testing:` precisaram de `()` no fim
  (`Suite/test()`) nesta versão — sem isso: "0 tests" verde falso.

### Achados REAIS desta vez (contraste com 0.83/0.85, onde ~tudo foi refutado)

1. **(Security HIGH-ish) `LaunchConfiguration.isOverridable` em Release aceitava
   `-ui-testing`/`XCTestConfigurationFilePath`** — ambos spoofáveis via `devicectl`
   com Developer Mode → `-force-premium-on` destravava premium em binário de loja.
   Fix: Release retorna `false` incondicional. (A justificativa "uncheatable" do ciclo
   0.83 estava ERRADA — launch args são input de atacante.) Docs RevenueCat anotados
   como Debug-only.
2. **(Correctness MEDIUM) `InsightService.fetchTodayActivityData` usava snapshot
   compartilhado SEM gate de frescor** — após a meia-noite o total de ontem inflava o
   insight de hoje (`max(liveSteps, today.steps)` + fallbacks). Reproducer vermelho
   (`dailyInsightIgnoresStaleSharedData`), fix = gate `isStale` igual ao
   `SmartNotificationService`.
3. **(Perf MEDIUM) `GoalService.goal(for:)` fazia fetch full-table POR CHAMADA** e o
   `StreakCalculator` chama por dia de streak (até 400×/refresh, main actor). Fix:
   cache `cachedGoals` invalidado em `setGoal` (todo acesso a StepGoal passa pelo service).
4. **(Tests HIGH) O guard de midnight do merge (fix do 0.85/126af15) era
   mutation-survivable** — nenhum teste falhava sem ele (o teste existente usava ontem
   MAIOR que hoje, o `max()` mascarava). Novos testes com dentes provados por sabotagem
   (vermelho sem o guard, verde com ele).
5. **(Security LOW) Team IDs + UDIDs de device em docs/memory tracked** apesar do
   sanitization pass de 2026-05-26; o guardrail `verify-device-identifiers.sh` não
   pegava UUID "nu" em prosa. Redigido + 2 padrões novos no script.
6. **(Tooling INFO) `test-payments-device.sh` passo 5 era código morto** — python -c
   com `\n` literais = SyntaxError garantido após o archive caro. Fix: heredoc.

### Demais mudanças

- Enhanced Security (skill `audit-xcode-security-settings`): projeto-level
  `ENABLE_ENHANCED_SECURITY` + PAC arm64e ATIVOS (build device 26.6 verificado);
  PAC=NO no watch (sem arm64e); `iOSPackagesShouldBuildARM64e` re-assertado pós-generate
  (RevenueCat é source-only, 0 binaryTargets). Os ENTITLEMENTS hardened-process v2 ficaram
  STAGED (não default): exigem capability Enhanced Security no profile da equipe, e
  regenerar o profile precisa de Apple ID logado no Xcode ("No Accounts" no CLI dos dois
  Xcodes → impossível autonomamente). Opt-in: `ENHANCED_SECURITY_ENTITLEMENTS=1
  Scripts/restore-entitlements.sh` após login interativo. Decision doc:
  `xcode-security-settings.md`. LIÇÃO: capability check de signing é guiado pelos
  entitlements, não pelo build setting — o A/B (entitlements plain + ES=YES) provou.
- WatchSync: branch `#else` morto (SharedStepData duplicado) deletado; context updates
  agora com throttle próprio + encode pulado quando nenhum canal está due.
- Widgets: `WidgetStepData` deletado → `SharedStepData` canônico (schemaVersion).
- TrainingPlanService: cache de planos (WorkoutsView fazia ~8-10 fetches por body pass);
  `WorkoutsView` @Query agora bounded (fetchLimit 6 + endTime != nil).
- Dead code: `generateGoalRecommendation` path (~80 linhas, zero callers),
  `fetchTodayActivityCount`, passo `registerBackgroundTasks` do AppStartupCoordinator
  (no-op permanente atrás do guard; o registro correto é no App.init).
- Formatters por render hoisted (DashboardView RelativeDateTimeFormatter, LiveActivity
  MeasurementFormatter, Logger ISO8601). `presentationMode` → `dismiss`/`isPresented`.
- `saveWorkout` pyramid dedup (`endCollectionAndFinish`, nonisolated por causa do
  protocolo @MainActor). Seams: `calculator` e `sendToWatch` injetáveis no
  StepTrackingService.
- Testes: AccessibilityModifiersTests deletado (XCTAssertTrue(true)); DynamicType/
  DesignTokens convertidos para Swift Testing (9 tautologias removidas; primeira
  utilização de `@Test(arguments: zip(...))` no repo); A11yID.Onboarding constants;
  TestUserDefaults falha alto em vez de cair em .standard.

### Deferred com justificativa (NÃO são esquecimentos)

- 4-B prefetch de upsert no HealthKitSyncService (cold-start only; metade do custo já
  morreu com o cache do GoalService).
- L5 Equatable em DailyStepSummary/WatchPayload (mecanismo de short-circuit alegado
  pelo reviewer é dúbio em Observation; benefício não comprovado).
- L6 split do WorkoutsView em structs (845 linhas; risco de regressão > benefício
  pré-release; M1 resolvido via cache no service).
- Refactors de timing em BackgroundTaskServiceTests/CoachServiceStreamingTests/
  TipJarStoreTests (suíte passa estável; rewrite tem risco próprio).
- Simplify skips: mover RelativeDateTimeFormatter p/ Formatters.swift (consumidor
  único, isolamento difere); "double call" de shouldSendReachableMessage no WatchSync
  (FALSO POSITIVO — estados de throttle distintos por canal).
