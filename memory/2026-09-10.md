# 2026-09-10

- Passe de auditoria autorizado (slop, performance, seguranca, DX de agente) sobre `eaf4060`, entregue em `9e6a84d` como 0.99 (55) e empurrado para `origin/master`. CI 34522119828 e CodeQL 34522120045 passaram nesse commit.
- Descoberta que reordenou o trabalho: o host so tem Xcode 27.0 (27A266a). Nao existe Xcode 26 nem `/Applications/Xcode-beta.app` (verificado com `mdfind` e `PlistBuddy`). `docs/agents/testing.md:15`, `docs/agents/build-and-dev.md:8` e `MEMORY.md:16` mandavam fixar `DEVELOPER_DIR=/Applications/Xcode.app` como "stable Xcode 26" — um no-op. A afirmacao de que a revisao fixada do RevenueCat quebra test builds no toolchain 27 e **falsa**: `build-for-testing` e a suite unitaria passam. O que falha e um destino *generico* de simulador, que compila arm64 e x86_64 e reporta `RevenueCat.swiftmodule ... built for incompatible target`. Quase certamente a origem do mito.
- Consequencia operacional: `Scripts/lib/xcode-toolchain.sh` falhava fechado em qualquer coisa que nao fosse 26.x, o que matava `e2e-simulator.sh`, `install-on-device.sh` e `test-payments-device.sh` na primeira linha — enquanto `Scripts/tests/xcode-toolchain.sh` continuava verde, porque exercita fixtures e nunca o host. Portao verde sobre capacidade morta. O seletor agora aceita 26.x ou 27.x, lista cada candidato rejeitado com a versao encontrada, e exporta `AIPEDOMETER_RESOLVED_XCODE_VERSION`/`_BUILD` para a evidencia ficar atribuivel. Criado `Scripts/preflight.sh` para checar o host, que e o que os testes de fixture nao conseguem.
- `project.yml` declarava 20 `SWIFT_UPCOMING_FEATURE_*`; o Xcode 27 define 19 no total e apenas 4 dos nomes declarados existiam. Provado por duas vias independentes: a lista autoritativa em `SWBUniversalPlatform.framework/Resources/Swift.xcspec` e o log de build, onde o compilador recebeu exatamente um `-enable-upcoming-feature ExistentialAny`. Os outros 3 reais eram implicados por `SWIFT_VERSION: 6.2`. `ENABLE_STRICT_CONCURRENCY` tambem nao e reconhecido; `SWIFT_STRICT_CONCURRENCY: complete` permanece.
- Performance, medida e nao suposta: Swift avalia expressoes de argumento default no *call site*, antes do corpo, entao o `guard allowsOverrides else { return false }` de `LaunchConfiguration` nunca impediu `ProcessInfo.processInfo.environment` de reconstruir um dicionario. Controle compilado com `swiftc -O`: 1000 avaliacoes para 1000 chamadas guardadas, 23,7 us por acesso com 87 variaveis. Esses leitores rodam dentro de corpos de modifier SwiftUI que reavaliam na cadencia do pedometro. Corrigido cacheando as proprias expressoes default, o que conserta todas as funcoes do arquivo sem tocar em nenhum call site.
- Erro proprio, relevante: a primeira versao do teste de regressao do GPX era vacua — afirmava `route.name.count <= GPXRouteParser.maxRouteNameCharacters`, comparando o resultado contra a mesma constante que o produziu, entao passava com a constante em 1.000.000. So apareceu porque rodei a metade vermelha neutralizando o limite. Corrigido para um literal. Ironia registrada: aconteceu dentro de uma mudanca cujo proposito era remover testes que nao podem falhar.
- Segundo erro: reportei "607/607 passando" lendo o log de uma execucao anterior enquanto a corrente tinha 1 falha. O validador do repo pegou. A falha era `Restarting after unexpected exit` — o processo de teste morreu em `PersistenceControllerTests` e reiniciou, o que tambem explica a contagem partida de 262. Re-execucao isolada: 9 testes, 1 suite, sem crash. Ambiental, sob disco quase cheio e host carregado, nao regressao.
- Revisao independente em duas vias (Standards/Spec, contexto fresco) achou 8 defeitos reais, todos corrigidos antes do commit: `Scripts/preflight.sh` nao rastreado, `MEMORY.md` afirmando 0.99 com `project.yml` ainda em 0.98, `CONTRIBUTING.md`/`TECH_STACK.md` com a alegacao falsa do RevenueCat, contradicao do diario em AGENTS.md, knob morto em `e2e-simulator-destinations.sh`, `L10n` ainda re-resolvendo o bundle, `currentLocale` sem usar o cache, e log temporario fixo em `/tmp` colidindo entre sessoes.
- Autoreview nao rodou: as cinco engines estavam indisponiveis (Codex sem cota, `pi` com token expirado, Claude sem creditos, `amp` ausente, `kimi` sem credencial). Registrado como portao nao cumprido, nao como revisao feita.
- Disco caiu de 12 GiB para 277 MiB durante o passe e derrubou a sessao par `fix-xcode-toolchain-mismatch`. Causa real identificada depois: 83 GB em `CoreSimulator/Devices` (26 dispositivos, os maiores com 10-13 GB cada), nao caches de Xcode — `DerivedData` inteiro eram 1,5 GB. Avisei duas vezes e continuei construindo; devia ter parado na primeira queda.
