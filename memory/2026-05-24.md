# 2026-05-24

## owner-iPhone install
- User asked to install the current app on `owner-iPhone`.
- Scope locked to `~/dev/PROJETOS_MOBILE/ai-pedometer`.
- Ran `DEVELOPMENT_TEAM=<LOCAL_DEVELOPMENT_TEAM_ID> bash Scripts/install-on-device.sh --device-name "owner-iPhone" --launch`.
- Physical Debug build succeeded and installed `com.mneves.aipedometer` on `owner-iPhone`.
- Launch failed after 3 retries because SpringBoard denied launch with reason `Locked`: `Unable to launch com.mneves.aipedometer because the device was not, or could not be, unlocked`.
- Independent install verification passed with `xcrun devicectl device info apps --device "owner-iPhone" --bundle-id com.mneves.aipedometer`: `AI Pedometer` version `0.78`, bundle version `34`.
- `xcrun devicectl device info lockState --device "owner-iPhone"` reported `passcodeRequired: true` and `unlockedSinceBoot: true`.
- Pre-existing dirty file left untouched: `memory/2026-05-20.md`.
