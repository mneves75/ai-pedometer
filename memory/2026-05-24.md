# 2026-05-24

## iMarcus install
- User asked to install the current app on `iMarcus`.
- Scope locked to `/Users/mneves/dev/PROJETOS_MOBILE/ai-pedometer`.
- Ran `DEVELOPMENT_TEAM=<LOCAL_DEVELOPMENT_TEAM_ID> bash Scripts/install-on-device.sh --device-name "iMarcus" --launch`.
- Physical Debug build succeeded and installed `com.mneves.aipedometer` on `iMarcus`.
- Launch failed after 3 retries because SpringBoard denied launch with reason `Locked`: `Unable to launch com.mneves.aipedometer because the device was not, or could not be, unlocked`.
- Independent install verification passed with `xcrun devicectl device info apps --device "iMarcus" --bundle-id com.mneves.aipedometer`: `AI Pedometer` version `0.78`, bundle version `34`.
- `xcrun devicectl device info lockState --device "iMarcus"` reported `passcodeRequired: true` and `unlockedSinceBoot: true`.
- Pre-existing dirty file left untouched: `memory/2026-05-20.md`.
