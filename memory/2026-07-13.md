# 2026-07-13

## 0.91 (47) — verification + fix cycle (committed d4a2958, pushed master)

Requested: full requirement verification + improve deep + fix all + bump/changelog/docs + commit+push.

### Shipped (static review only — see caveat)
- **Distance badges now earnable.** distance5km/10km/Marathon were rendered in BadgesView but absent from `BadgeDefinitions.all` → no award path. Added meter thresholds (5_000/10_000/42_195, all < Int32.max for watch arm64_32) + a `distance` branch in `StepTrackingService.evaluateBadges(steps:streak:distance:)`, fed the day's distance at all 3 refresh sites (HK, wheelchair, motion). Mirrors the steps/streak branch. Reproducer: `AIPedometerTests/Badges/BadgeDefinitionsTests.swift` (award-path invariant + 32-bit guard). NOTE: interpreted as **daily** distance (consistent with daily steps badges), not lifetime cumulative — the "walk a total of N km" copy is slightly imprecise but strings left unchanged to avoid xcstrings churn.
- **monthlyChallenge (.challenge) hidden** in BadgesView (no challenge system exists; filter keeps it if a historical record holds it).
- **MARKETING_VERSION float-trap fixed + bump.** `project.yml` had `MARKETING_VERSION: 0.90` unquoted → YAML/XcodeGen float 0.9 → pbxproj `MARKETING_VERSION = 0.9` (App Store sorts below 0.89). Now `"0.91"` (47). VERIFIED quoted in generated pbxproj.
- README "bidirectional" watch sync → "mirrors the phone's daily step snapshot" (sync is phone→watch only).
- AGENTS.md `## GUIDELINES-REF` re-synced to `~/dev/GUIDELINES-REF/AGENTS.md` (check-agents-sync gate was RED; now green — pre-commit hook confirmed).
- CHANGELOG 0.91; asc-validate version refs bumped in README/test_plan/docs/agents.

### VERIFICATION CAVEAT (important)
Full simulator build+test was **NOT run** for this commit. Committed on static review + non-build gates only (entitlements valid, xcstrings JSON valid, agents-sync green, device-id clean, version metadata correct). User explicitly chose "commit+push now, static only" after the environmental block below. **Before any release/TestFlight: run `bash Scripts/e2e-simulator.sh` (or -only-testing:AIPedometerTests) once host is idle to confirm green.**

### Environmental landmine — host build deadlock at high load (NEW)
- An `e2e-simulator.sh` build-for-testing **wedged ~7h44min at 0% CPU** (log frozen 15:53, ELAPSED 07:48, only 29s CPU consumed), stuck at a `clang -v -E -dM` compiler probe. This is a **deadlock**, not slowness (0% CPU).
- Root cause: **host load average hit 370→410**, driven NOT by Swift builds (zero xcodebuild/swift running) but by **Microsoft Defender `wdavdaemon` (~194% CPU) + CloudKit `cloudd` (~130%) + Spotlight `mds` + ~8 concurrent Claude Code sessions**. Defender real-time scanning of build-artifact churn most likely held a lock on a build intermediate mid-compile → deadlock. Even `restore-entitlements.sh` (trivial echo+plutil) timed out at 2min.
- LESSON: at load >~100, do NOT launch simulator builds — they deadlock for hours and add churn that feeds the antivirus/Spotlight fire. Check `uptime` + `ps -r` (look for wdavdaemon/cloudd/mds) BEFORE building. Recovery: `git checkout -- <entitlements>` restores the correct (committed) entitlement state without the hanging plutil path in restore-entitlements.sh.
- The 8h-wedged build left entitlements blanked (`<dict/>`) after xcodegen ran but restore couldn't complete under load. `git checkout` of the 3 `.entitlements` files = correct restore (committed state IS the restored state for the non-hardened variant).

### Build-deadlock root cause REFINED (supersedes the "load 410" theory)
- The `xcodebuild` hang is DETERMINISTIC and watch-specific, NOT primarily load-driven. Two separate runs (host load 410 AND host load 10) both wedged at the IDENTICAL point: `SWBBuildService` during `CreateBuildDescription`, right at the `clang -v -E -dM -isysroot .../WatchSimulator26.5.sdk` probe, 0% CPU.
- PROVEN not the toolchain: running that exact `clang ... WatchSimulator26.5.sdk` probe standalone returns instantly (exit 0, 497 macro lines). So the hang is in the build SERVICE's build-planning of the multi-target (watch-inclusive) scheme — most likely `GatherProvisioningInputs` for the watchOS target, not clang.
- Workaround attempted: dropping `- target: AIPedometerWatch` from the app deps in project.yml (memory-sanctioned) to build iOS-only. Could NOT confirm it works because the regen step timed out (see below). NOTE: removing the app's *dependency* on the watch may not remove the watch target from the scheme build set anyway — the `AIPedometer` scheme lists only the AIPedometer/Tests/UITests targets to build, so the watch enters the plan only via the app's embed dependency; removing it SHOULD drop it, but unverified.
- `xcodegen generate` chains `postGenCommand: bash Scripts/restore-entitlements.sh` (project.yml line 9). restore-entitlements.sh's `plutil` calls hang under host load, so `xcodegen generate` APPEARS to hang for 2min+ when the real culprit is the chained plutil. Under low load it completes. To regen without the plutil path when needed, run xcodegen and `git checkout` the entitlements instead (they commit in the restored state).
- Host also oscillates hard: load 10 → 102 → 410 across hours, driven by Microsoft Defender + CloudKit + Spotlight + multiple CONCURRENT Xcode-BETA clang builds from OTHER Claude sessions/repos (I use stable Xcode; the beta clangs are not mine). Piling more builds on worsens everyone's deadlocks.
- NEXT-TIME PLAYBOOK: (a) confirm host is quiet (`uptime` < ~15, no foreign `clang`/`xcodebuild`); (b) `xcodegen generate` (let postGenCommand run at low load); (c) try `CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` on the xcodebuild invocation — if the hang is GatherProvisioningInputs, this may skip it; (d) if still hung at the watch probe, drop the watch dep, regen, build iOS-only, restore. Device install (imarcus) uses the watch DEVICE SDK path (different from WatchSimulator) — may or may not hit the same hang; test separately.

### Orchestration notes
- 7 Claude `improve deep` audit subagents + the traceability subagent all hit **session limit (reset 20:20)** mid-run. Traceability matrix (22 reqs) completed before dying; the 7 category auditors did not return. Fell back to inline vetting of already-confirmed findings.
- Prior-cycle rule held: subagent findings vetted against real code before acting. Of the traceability "ALTA" claims: badges (CONFIRMED bug), Expedition premium recheck (impl correct, only a test gap), watch-sync one-way (doc drift not bug), widget decode-path (refuted as bug — behaviorally identical to canonical `UserDefaults+AppGroup`).

### Still open (NOT done — deferred by environment/quota)
- Real green build+test run (unit + UI) — REQUIRED before release.
- Fase 4 interactive simulator walkthrough (argent MCP disconnected mid-session + host load) — not done.
- autoreview skill not run (needs build; host deadlocked).

## improve deep audit — COMPLETED (later 2026-07-13, quiet host)
Re-ran the audit via 4 read-only Explore subagents (concurrency / 32-bit+time / security / correctness+test-gaps) after the earlier Codex run produced no findings file (it read source to 8437 log lines then exited without writing output/codex-audit-findings.md — no last-message; root cause unconfirmed, likely turn/token exhaustion). All findings VETTED against real code by me. Result: codebase is exceptionally hardened.
- **Written to `plans/`** (advisor deliverable, NOT committed — build-blocked, can't verify): README + 001-005.
  - 001 BUG-01 (MED, real): `discardWorkout` save-failure leaves `session.deletedAt` set in memory with no rollback → a subsequent `finishWorkout` persists a finished-AND-soft-deleted row that vanishes from every `deletedAt == nil` query. Exact patch (snapshot+restore, mirrors `TrainingPlanService.applyPlanMutation:326-343`) + exact reproducer inlined. `.discardFailed(msg).id == "discardFailed-\(msg)"`.
  - 002 TEST-01 (HIGH): `SmartNotificationServiceTests` tests 1&2 build the service against real `UserDefaults.standard`+app-group (no injected defaults); test 3 injects `userDefaults` but not `sharedUserDefaults` → order-dependent flake + pollution. Fix: inject `TestUserDefaults` into all 3.
  - 003 TEST-02, 004 TEST-03 (widget loader), 005 CONCURRENCY-01 (serialize refreshWeeklySummaries/refreshStreak through refreshChain — staleness not corruption).
- **CLEAN / rejected** (don't re-audit): 32-bit overflow class (only the fixed ConfettiView UInt32 hash), all date/boundary math calendar-based+guarded, 0.89 non-@Sendable callback class clean everywhere, continuations single-resume, GPX parser XXE-off+caps, logs redacted (`Logger.renderPayload` → "[private]"), premium fail-closed, zero network egress, zero `try!`/`as!` in app code, app-group decode guarded. SEC-01 SwiftData `untilFirstUnlock` = by-design (widgets read while locked). `ProgressClamp.percent` no high-side clamp = unreachable (>21.4M× goal), non-finding.

## BUILD DEADLOCK — DEFINITIVE ROOT CAUSE (supersedes prior theories)
`sample` of the wedged process nailed it: the `clang -v -E -dM -isysroot .../iPhoneSimulator26.5.sdk -x c -c /dev/null` compiler probe that **SWBBuildService** spawns during `CreateBuildDescription` hangs **100% in a `write()` syscall** (`Command::Print → raw_fd_ostream::write_impl → write`), 0% CPU, for minutes — the child's stdout/stderr pipe fills and SWBBuildService never drains it. Classic pipe-buffer deadlock between the build service and its own external-tool probe. The SAME clang run standalone returns in 0.038s exit 0, so the toolchain is fine. Reproduced 4× this session, independent of load (host was ~13 AND ~410, identical wedge point). **Microsoft Defender Endpoint Security ext `epsext` (PID 32014, hooking AUTH_EXEC) is the prime suspect** — ES exec-hook interference on the spawned clang. `CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` got past GatherProvisioningInputs (ruled that out) but still wedges at the clang probe. NOT an AIPedometer code defect. Mitigation requires excluding `~/Library/Developer/Xcode` (+DerivedData) from Defender real-time scanning — admin/MDM, out of session scope. Until then, simulator build/test/install (incl. iMarcus) cannot be verified on this host. Recovery to unwedge: `pkill -9 -f "xcodebuild -scheme AIPedometer"; pkill -9 SWBBuildService`.

## Plans 001-005 implemented and verified — supersedes the blocker conclusion above

- The observed failure boundary remains the capability-probe child blocked in `write()` on a full
  pipe. Defender causality was never proven. A scoped workaround that terminates only the blocked
  stable-Xcode probe child lets `SWBBuildService` continue; broad `pkill` is unnecessary and unsafe.
- Implemented transactional workout save-failure recovery, isolated notification defaults, canonical
  app/widget shared-data persistence, and latest-request-wins weekly/streak refreshes.
- TDD evidence: new workout/shared-persistence/refresh reproducers failed before the production fixes.
  Final verification on Xcode 26.x with an explicit iOS 26.5 simulator: 476/476 unit tests and 16/16
  UI tests passed.

## Physical-device closeout

- Stable-Xcode Debug device build succeeded with the iPhone app, widget extension, and watch app
  signed and embedded. Installed AIPedometer 0.91 (47) on iMarcus.
- The first post-install launch ended when the phone auto-locked; console repro returned
  `FBSOpenApplicationErrorDomain error 7` rather than a crash. After keeping the screen unlocked,
  launch succeeded and the same process remained present at both 5 and 20 seconds.
- `install-on-device.sh` then exposed a separate post-build hang: it captured the full
  `-showBuildSettings` output in memory and passed the large string through here-strings. The shell
  was sampled blocked in `heredoc_write`. The script now writes build settings to temporary files,
  parses those files directly, and cleans them on exit. The large-output iPhone/watch fixture passes.

## Second improve-deep pass — bounded fixes

- Reproducer-first fixes: terminal workout single-flight; rollback of failed goal/badge/training-plan
  inserts; historical goal resolution in TrainingPlanService and HealthKitDataTool; Release-only
  rejection of test/demo/reset and RevenueCat environment overrides.
- Least privilege: the watch target uses WatchConnectivity only, so unused HealthKit and app-group
  entitlements plus the watch HealthKit usage string were removed.
- CI/DX: macOS runner selects Xcode 26.3, CodeQL builds the generated project explicitly, script
  tests run in CI, `.xcresult` validation rejects zero/failing/non-success runs, and Debug returns to
  incremental compilation while Release remains whole-module.
- Remaining high-risk work is deliberately planned rather than improvised: durable idempotent
  HealthKit workout export, real query-adapter tests, batched daily-record upserts, and measured
  shared-data write coalescing (`plans/006`–`009`).

## Final validation pass

- Full Swift Testing suite: 483/483 passed on the explicit iOS 26.5 simulator. Full XCUITest suite:
  16/16 passed. The SwiftUI tab bar omitted some `Label` identifiers from concrete buttons, so the
  driver now falls back to the stable tab ordinal and still proves the destination with its screen
  marker. The isolated reproducer and the full UI suite both passed.
- Release simulator build succeeded and validated both the embedded watch app and widget extension.
  The known `SWBBuildService` pipe deadlock recurred; terminating only its two blocked compiler-probe
  children let the unchanged build continue to `** BUILD SUCCEEDED **`.
- Post-review fixture cleanup initially failed compilation because a multi-statement function needed
  an explicit `return`; the targeted `AppConstantsTests` rerun then passed 14/14 with a validated
  `.xcresult`.
- Final `autoreview` reported no actionable findings (confidence 0.87). An intermediate claim about
  empty arrays under Bash 3.2 did not reproduce on the supported `/bin/bash` 3.2.57: direct expansion,
  the isolated trap, and installer `--help` all exit cleanly. A regression test now preserves the
  `--help` path.
- All eight shell test scripts, four Python result-parser tests, `actionlint`, Bash syntax, AGENTS
  sync, device-identifier scan, entitlement validation, `plutil`, and `git diff --check` passed.
- A device build exposed that orientation settings in `project.yml` did not reach the manual
  `AIPedometer/Resources/Info.plist` because `GENERATE_INFOPLIST_FILE=NO`. A regression test first
  failed on both missing arrays; after moving the four orientations into the source plist and
  removing the ineffective build settings, the targeted suite passed 6/6 and its `.xcresult`
  validated successfully.

## Final iMarcus install

- Stable Xcode built and signed the full Debug device bundle after the same scoped compiler-probe
  workaround. Embedded widget and watch binaries passed Xcode validation.
- Installation succeeded and `devicectl device info apps` independently confirmed AI Pedometer 0.91
  (47) on iMarcus. The final device bundle contains all four orientations for iPhone and iPad, and
  `codesign --verify --deep --strict` passed.
- Launch is not proven in this final pass: all three earlier installer attempts plus two direct
  `devicectl` attempts were rejected by SpringBoard with `FBSOpenApplicationErrorDomain error 7` /
  `Locked`.
  The remaining action is physical unlock followed by launch; installation itself is complete.

## 0.92 (48) — final production-hardening closeout

- Implemented plans 006–009 and all justified deep-review findings. HealthKit workout export is
  durable, idempotent, single-flight, cancellation-aware, and fair across retry batches; V1 legacy
  rows migrate without being blindly re-exported. Shared payload writes coalesce latest-value state,
  invalidate obsolete timers, bound wall-clock rollback delay, and flush at lifecycle/widget edges.
- Final stable-Xcode simulator evidence: 504/504 Swift Testing tests and 16/16 XCUITests passed;
  `analyze` and the Release build succeeded, including embedded watch/widget validation. The final
  visibility cleanup also passed 22 targeted configuration tests.
- Security/operations: worktree and 153-commit `gitleaks` scans found no leaks; AGENTS sync,
  device-identifier, entitlement, plist, `actionlint`, `shellcheck`, `git diff --check`, ten shell
  regression scripts, and four Python parser tests passed. A large-output scanner deadlock and a
  malformed-entitlements false pass were reproduced before their fixes and passed afterward.
- Split `autoreview` passes left core and app clean (confidence 0.84). Operations found the valid
  malformed-plist gate above; the final rerun was attempted after the fix but the review engine hit
  its usage limit. Targeted and full script gates therefore provide the final evidence for that
  shell-only delta.
- Stable Xcode built, signed, and installed AI Pedometer 0.92 (48) on iMarcus. The install script
  launched it successfully, `devicectl` independently confirmed the installed version, and a
  separate process query found the app running. The two known stuck compiler-capability probes were
  terminated individually; the unchanged build service then completed successfully.
- Remaining manual observations: force a real HealthKit export failure/retry/relaunch and capture a
  representative walking signpost trace. These do not represent missing production code or failed
  automated gates.
- The first post-push CI run proved that the macOS runner did not preinstall `ripgrep`; CI now
  installs that declared dependency. The next run reached all 16 UI tests and exposed a transient
  `Timed out while acquiring background assertion` simulator failure that the retry classifier did
  not recognize. A red/green classifier regression now covers it, and the CI budget is 60 minutes
  so one bounded simulator retry can complete.
- A superseded CodeQL run reached the 45-minute job limit, and a concurrent intermediate run exposed
  a version mismatch between the pinned CodeQL action and the runner-managed configuration. The
  workflow now pins the official v4.37.0 commit consistently, allows 90 minutes for instrumented
  Swift compilation, and serializes same-ref analyses so obsolete pushes do not consume macOS
  runners in parallel.

## 0.93 (49) — physical HealthKit retry closeout

- A real iMarcus workout completed with HealthKit Workouts write permission temporarily disabled.
  Privacy-safe aggregate store inspection proved one completed local row, one pending export, one
  recorded failure, one stable export ID, and no HealthKit ID.
- Restoring the original permission and relaunching 0.92 preserved the pending row but did not retry
  it: the six-hour daily-sync throttle also skipped pending reconciliation. A reproducer was RED
  before adding `HealthKitSyncService.performAutomaticSync()`.
- The fix preserves the expensive sync throttle while always running the lightweight durable workout
  reconciliation at startup/foreground. A local preflight skips HealthKit authorization when there
  is no pending export. The 26-test HealthKit sync suite passed. Installing the
  fixed build over the same device container changed that row to exported with zero failures; a
  second relaunch kept one row, one stable export ID, and one HealthKit ID. The app process remained
  alive and the Workouts permission was restored to ON.
- Release metadata advanced to 0.93 (49). The representative walking signpost trace remains deferred
  because the user chose not to perform the required two-minute physical walk in this session.
