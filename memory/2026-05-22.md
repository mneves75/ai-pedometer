# 2026-05-22

## Architecture cleanup continuation

- Started continuation toward "rewrite / implement a better, concise and elegant code" using the `improve-codebase-architecture` skill.
- Scope locked to `/Users/mneves/dev/PROJETOS_MOBILE/ai-pedometer`.
- Current dirty state before edits: `memory/2026-05-20.md` already modified; treat it as pre-existing user/worktree state.
- Selected the Routes & GPX file-import path as the next deepening opportunity: `WorkoutsView` had a shallow module interface because UI code also knew security-scoped URL access, file-size preflight, mapped reads, parsing, and storage.
- Added `Shared/Utilities/GPXRouteImporter.swift` so `WorkoutsView` now calls one import interface and keeps only UI state transitions.
- Regression evidence:
  - Before implementation, targeted `GPXRouteParserTests` failed to compile with `Cannot find 'GPXRouteImporter' in scope`.
  - After implementation and `xcodegen generate`, targeted `GPXRouteParserTests` passed with 11 Swift Testing tests.
  - `WorkoutsViewTests` passed with 1 Swift Testing test.
- Second cleanup target: `WorkoutsView` also owned active training-plan to workout-card projection.
- Added `TrainingPlanRecord.currentWorkoutRecommendation` and `currentWorkoutRecommendationSummary` so current-week target selection, intent mapping, difficulty, estimated minutes, and summary fallback live behind the model interface.
- Regression evidence:
  - Before implementation, targeted `TrainingPlanRecordTests` failed to compile with missing `currentWorkoutRecommendation` / `currentWorkoutRecommendationSummary`.
  - After implementation, targeted `TrainingPlanRecordTests` passed with 7 Swift Testing tests.
  - Full `xcodebuild -scheme AIPedometer -destination 'platform=iOS Simulator,name=iPhone 17' test` passed: 16 XCTest unit tests, 435 Swift Testing tests, and 15 UI tests.
