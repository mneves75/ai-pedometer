# 2026-05-26

## Session Log

- Started physical-device install request for `iMarcus`; scope locked to `/Users/mneves/dev/PROJETOS_MOBILE/ai-pedometer`.
- Preflight confirmed `iMarcus` is available and paired through `xcrun devicectl list devices`; using the repo install script with the local development team override.
- Physical-device install succeeded with `DEVELOPMENT_TEAM=<LOCAL_DEVELOPMENT_TEAM_ID> bash Scripts/install-on-device.sh --device-name iMarcus --launch`; first launch retry was blocked by lock state, second launch succeeded, and `devicectl device info apps` confirmed `AI Pedometer 0.78 (34)` installed.
- Follow-up hardening pass after `f19d2f9`: replaced the UI-test hardcoded onboarding accessibility-label list with a string-catalog lookup so the test follows `Localizable.xcstrings`.
- Targeted `testOnboardingCapturesScreens` passed after the helper change.
- `xcrun devicectl list devices` still showed `iMarcus` available and paired, but `xcrun devicectl device info apps --device iMarcus --bundle-id com.mneves.aipedometer` timed out with CoreDevice connection error 4000 / Network.NWError 60, so installed-app metadata was not refreshed in this pass.
- `codex-bypass-exec review --uncommitted` flagged hardcoded local Apple team IDs in memory notes; repo-local memory notes now use `<LOCAL_DEVELOPMENT_TEAM_ID>` instead.
