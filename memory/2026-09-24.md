# 2026-09-24

- Nome do dono: não aparece mais em nenhum site do estúdio/apps (verificado ao vivo: 15 páginas com 0
  ocorrências, controle positivo no mvneves.dev com 2). mvneves.dev e perfil do GitHub mantêm o nome
  (decisão do dono, depois de respostas divergentes a duas sessões). LUME 1.16.3 publica o Encarregado
  como conhecendotudo.com.br + canal (risco do art. 9º §1º II da Res. ANPD 18/2024 aceito pelo dono).
  Neste repositório: LICENSE → conhecendotudo.com.br, device → owner-iPhone (`d04313e`).
- CI falhou em `d04313e` (E2E iOS 26.2, auditoria de acessibilidade). Reproduzido num simulador iOS 26.5:
  ícone decorativo do banner de IA indisponível lido pelo nome do símbolo (bug real) + contraste do texto
  do card de GPX (falso positivo do iOS 26). Ícones escondidos, cabeçalho do card vira container,
  isenção por identificador. 1.0.8 (66) no TestFlight, tag `v1.0.8-beta2`; CI verde em `2bff42b`.
- Screenshots da App Store 1.0.8 enviados ao ASC (pt-BR e en-US, 22 de 22 COMPLETE): iPhone 6.5" (7 telas,
  redimensionadas de capturas 6.9") e iPad 13" (4 telas), build Debug de `426e3f7` com dados de demonstração
  e IA real no aparelho; `appstore-screenshots-validate.sh` verde. Insights de IA com porcentagem errada
  foram regerados antes da captura. Sem Tip Jar nem Treino Ativo: o simulador não carrega preço nem inicia
  sessão de treino. `asc validate` agora só bloqueia em itens do dono: 24 campos de classificação etária,
  4 de contato da App Review e direitos de conteúdo.
- Achado: no iPad 13" a introdução estica o cartão e o botão de borda a borda e deixa a metade de baixo
  vazia; falta largura máxima de leitura. Ficou fora das capturas do iPad.
- Armadilhas de simulador: outra sessão abriu o app dela no simulador em uso (o contato visual pegou uma
  captura errada) e algo externo desligou os dois simuladores; iPadOS 27 abre em "Aplicativos em Janelas",
  que põe nome do app e alça de redimensionar na captura (trocar para "Apps em Tela Cheia").
- Ciclo de revisão da 1.0.8 (67), base `207ee39`: code-review (Standards + Spec), revisão de corretude e
  auditoria de segurança (nenhum achado crítico/alto/médio; GPX e proteção em repouso já eram não-achados).
  Ledger completo em `agent_planning/2026-09-24-review-security-ship/GOAL.md` (local).
- Coach IA (capturas do dono): o modelo on-device nunca chamava `fetchActivityData` e pedia ao usuário o
  argumento `days`. Agora o 1º turno leva 7 dias, meta e sequência (renova após 30 min/dia novo/sessão nova),
  com totais calculados pelo app e cabeçalhos localizados. Replay com o modelo real
  (`Scripts/coach-grounding-eval.swift`): antes 13/20 pediam dados e 1/20 citava; depois 20/20 citam com média
  certa e 0 vazamento de inglês. A 1ª versão vazava inglês em 8/16: só a medição pegou.
- Ajustes gravava `false` nos lembretes sem permissão de notificação (inclusive `.notDetermined`), violando o
  invariante. Agora só lê o status e mostra aviso. Retomada do lembrete suspenso com dono único e geração
  compartilhada. Autoreview (GPT-6 Astra, P3) limpo; minha revisão achou "Permitir Notificações" desligando o
  lembrete com IA indisponível; mneves-verify #1 FAIL achou geração dupla (toggle + retomada), texto em inglês
  fixo e o script em pasta ignorada. Corrigidos com teste vermelho antes.
- Host com carga média até 153: a auditoria de acessibilidade e o teste seguinte (SIGTERM) falharam na rodada
  completa de UI e passaram isolados.
- ASC: não existe nenhuma compra no app; a gorjeta só existe no `.storekit` local e aparece "indisponível" fora
  do Xcode. Criar o consumível depende do preço (decisão do dono).
- mneves-verify #2 (GPT-6 Astra, contexto novo, snapshot isolado): FAIL. Critérios 2-4 PASS (652 unit, 31 UI
  iPhone + 3 iPad). Restou 1/8 resposta que citava os dados mas ainda dizia "não consigo acessar o app Saúde"
  no turno "Pesquise no app health"; `%lld` nas contagens de dias; avisos `dsymutil` no Release (ruído de
  toolchain). Laço de verificação encerrado. Depois dele, sem nova verificação independente: mensagem que cita
  Saúde/Health re-ancora o turno (teste vermelho em 4 casos) e dias via `Formatters`. Replay final: antes 14/20
  pediam dados, depois 0/20, 20/20 citando os dados e 0 vazamento de inglês.
