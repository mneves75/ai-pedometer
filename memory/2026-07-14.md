# 2026-07-14

## 11:07 BRT

- Started a repository-guidance update requested by the user for `AGENTS.md` and `CLAUDE.md`.
- Scope: add syntax-aware `ast-grep` defaults, codebase lint/hook expectations, verification and bug-reproduction workflow, security constraints, Apple platform documentation paths, project memory/field-guide rules, and model-routing guidance while deduplicating existing policy.
- Validation boundary: documentation/config guidance only; verify readback, diffs, sync checks, and autoreview. Browser UI QA is not applicable unless the change affects a rendered artifact.
- Added a Swift ast-grep project with blocking rules for `try!` and `as!`, rule tests, and pre-commit enforcement. The repository already had `core.hooksPath=.githooks` configured.
- Validation wrapper mistake: used zsh's read-only `status` variable after otherwise successful checks, which prevented the tmux completion signal. Use a neutral name such as `exit_code` in future wrappers.
- The main worktree changed concurrently during two autoreview attempts, expanding from 3 unrelated modified files to 22. Preserved every concurrent change and moved review to an isolated temporary worktree.
- Isolated autoreview found a valid partial-staging bug: scanning the working tree could miss a violation present only in Git's index. The hook now materializes the exact staged snapshot with `git checkout-index`, fails closed if staged `sgconfig.yml` is missing, and has an alternate-index regression test proving staged-only violations are inspected and rejected.
- A follow-up isolated review found that ast-grep's default ignore processing could still skip a tracked staged file. The snapshot scan now disables hidden, dotfile, exclude, global, parent, and VCS ignores; a real-ast-grep regression stages an ignored Swift file with `try!` and requires the `swift-no-force-try` diagnostic.
- The final sparse-checkout review found that `git checkout-index --all` omits entries marked `SKIP_WORKTREE`. The hook now uses `--ignore-skip-worktree-bits`, and the staged-only regression marks its fixture `SKIP_WORKTREE` to prove the complete index is materialized.

## 13:12 BRT

- Release candidate finalized as `0.93 (49)` with automatic pending-workout reconciliation outside the six-hour full-sync throttle.
- Regression evidence: the focused automatic-sync test failed before the fix and passed after it; the full `HealthKitSyncServiceTests` suite passed 26/26.
- Final simulator E2E passed on the release source tree: 506/506 unit tests, 16/16 UI tests, plus widget and watch builds. `xcodebuild analyze` also passed.
- Security closeout found no actionable finding: the tracked tree and full Git history passed the secret scans, and native-code review found no applicable web injection, route, CORS, or TLS-bypass surface.
- Physical HealthKit failure recovery was exercised with Workouts permission temporarily disabled, then restored. The retry exported the single pending record with stable identifiers and did not duplicate it.
- Release build `0.93 (49)` installed on `iMarcus`; launch was accepted and a separate post-launch process check passed. The optional two-minute physical walking trace was deferred by the user.
- Privacy cleanup: retained only the redacted smoke log and removed the temporary device-info JSON before commit.
- Browser QA is not applicable because the changed product surface is native iOS and the remaining changes are tests, docs, and tooling.
