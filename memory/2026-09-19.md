# Revisão AIPedometer 1.0.4 (60) — em andamento

Baseline: `219fe46`, master inicialmente limpa. Publicação, pagamentos reais,
assinatura e certificação de produção estão fora desta entrega autorizada.

## Ambiente e evidência local

- Xcode estável 27.0 (27A266a), selecionado por `DEVELOPER_DIR` e
  `AIPEDOMETER_XCODE_DEVELOPER_DIR`; seletor global aponta o beta 27.2 instalado.
- Preflight passou sem avisos. DerivedData exclusivo `AIPedometer104`.
- Baseline unitária: 661/661; UI inicial: 22/22, sem falhas ou skips.
- SmartNotification: vermelho 3/11 falharam; verde 11/11 após validar mensagens
  e não estimar distância de cadeira de rodas por passada.
- Datas/exportação: vermelho 2/27 falharam; apresentação: 4/32 falharam.
  Verde combinado 49/49 após corrigir início da sequência, localização indoor
  no HealthKit, rollover compartilhado/watch e nomes localizados de Live Activity.
- Catálogo: vermelho 1/3 falhou por quatro chaves ausentes; verde 2/2.
- Inicialização/onboarding: vermelho 3/10 falharam; verde 10/10 após correções.
- Integração unitária em 1.0.4: 674/674, 86 suítes; UI pt-BR: 23/23.
  Ambos os xcresults foram validados, com zero falhas e zero skips.
- Coach: duas novas regressões falharam em 19 testes, com três asserções;
  correção de ownership por geração passou nos 19/19 testes, sem skips,
  com xcresult validado.
- App Debug compilado confirmou `CFBundleShortVersionString=1.0.4` e
  `CFBundleVersion=60` no app, watch e widgets; isso não comprova produtos
  archive. Build watchOS `arm64_32` passou sem assinatura. Release arm64
  passou; seus três produtos também confirmaram 1.0.4 (60).
- Regressões de scripts, shellcheck, actionlint, dois casos ast-grep e onze
  testes Python do validador passaram. `pnpm audit` incluindo desenvolvimento
  terminou sem vulnerabilidades conhecidas.
- `plutil -lint` passou nos três manifestos de privacidade da fonte e nas
  cópias empacotadas nos três produtos Release do simulador; essa verificação
  prova sintaxe/presença, não certificação de privacidade de produção.

Evidências volumosas ficam em `.scratch/aip104-20260919/`; estado de execução e
inventário em `agent_planning/2026-09-19-aipedometer-1-0-4-Bs9x1g/`.

## Decisões e limites

- Hipóteses de formatação regional do resumo e acessibilidade da meta não se
  reproduziram; testes passaram antes de qualquer mudança de produção.
- RevenueCat 5.81.1 retido após comparação com 5.90.2: nenhuma necessidade
  aplicável demonstrada. Freshness retorna 10 (não é um gate verde); integridade
  do pin passou. Metadados públicos vieram de `gh api` após HTTP 403 no curl.
- Documentação corrigida: paywall usa pacotes nativos, não `PaywallView`;
  RevenueCatUI continua usado pelo Customer Center.
- Auditoria de segurança em fonte, 43 unidades; críticos e validação ainda
  pendentes. Provas adversariais não executadas sem o isolamento exigido.
- Argent abriu onboarding e permitiu inspeção da imagem, mas árvores de
  acessibilidade vieram vazias no iOS 27. Não foram usados taps derivados de
  pixels. QA manual completo ainda pendente; XCUITest funcionou normalmente.
  Reiniciar o transporte recuperou a árvore do SpringBoard, mas não a do app
  ou HealthKit. Capturas XCUITest de onboarding, painel, treinos e treino ativo
  foram inspecionadas em pt-BR; textos e botões visíveis não estavam truncados.
  Essa inspeção não equivale à validação completa de acessibilidade.
- Primeira tentativa de build sob sandbox saiu 74; execução autorizada fora
  desse sandbox passou. Primeiro ast-grep sem `--skip-snapshot-tests` falhou
  por snapshots ausentes; repetição com o comando canônico passou.
- Compatibilidade iOS 26.5: 699/699 (676 unitários e 23 UI), sem falhas/skips,
  xcresult validado. Release para destino concreto tentou também x86_64 e saiu
  65 por módulo RevenueCat arm64 incompatível; repetição limitada a arm64 passou.
- iPad Pro 13 M5/iOS 27: execução inicial de UI terminou com 14/23 aprovados,
  nove falhas e nenhum skip. Oito falhas procuravam a aba compacta Mais no
  layout com sidebar; o driver agora usa os destinos diretos de Ajustes e
  Conquistas. A falha restante ao pular onboarding está sob investigação.
  A repetição passou em 22/23, isolando a falha de Pular. Captura e árvore
  mostraram botão 33×16 com centro sobreposto ao indicador do paginador;
  nenhum alerta de persistência estava presente. Ampliar o label interativo
  para 44×44 resolveu a regressão (1/1 validado). A suíte completa seguinte
  passou em 23/23, zero falhas/skips, bundle validado, sem remover testes ou
  asserções. Captura final do onboarding também inspecionada.
- Unitária iOS 27 após integração do Coach: 676/676, zero falhas/skips, bundle
  validado. Análise estática passou; posterior alteração do alvo de toque no
  onboarding ainda requer gate afetado atualizado no candidato final.
- Capturas pt-BR do iPad: painel e configurações inspecionados; sidebar e
  textos visíveis sem sobreposição/truncamento. Não equivale a auditoria
  completa de acessibilidade ou validação física.
- UI iPhone/iOS 27 repetida após as mudanças de navegação e onboarding:
  23/23, zero falhas/skips, xcresult validado.
- Repetição UI iOS 26.5 após mudanças de navegação/onboarding: 23/23,
  zero falhas/skips, bundle validado.
- Preferência HealthKit no cálculo da sequência: regressões funcionais com
  fakes falharam 2/39 antes da correção; depois passaram 39/39, incluindo
  preferência desligada, resultado pendente e desligar/religar. O controle
  habilitado continua gerando a sequência. Dados já persistidos preservados.
  Isso não demonstra revogação de autorização do SO ou entrega real ao watch.

- Planos: teste funcional vermelho 16/18 (duas falhas), verde 18/18 após
  autorização no serviço. Novas gerações exigem acesso autorizado; revogação
  durante awaits impede persistência; estado desconhecido não revoga trabalho
  admitido nem apaga planos. Bundles `premium-plan-{red,green}.xcresult`.
- Segurança: oito candidatos receberam validação independente; quatro refutados
  e quatro ainda em verificação final. Registros estruturados e ledger de 43
  unidades passaram nos validadores. GPX e estreitamento numérico do HealthKit
  continuam sem prova de impacto no runtime; não são vulnerabilidades confirmadas.

- Suítes unitárias finais após autorização/sincronização: 681/681 no iOS 27 e
  681/681 no iOS 26.5, sem falhas/skips, ambos os bundles validados.
- Auditoria de segurança concluída em fonte: 43 unidades, oito candidatos
  validados independentemente, seis refutados no candidato atual e dois
  `needs_validation` (recursos de XMLParser e limites numéricos reais de
  HealthKit). Nenhuma vulnerabilidade confirmada. Verificação final e os dois
  registros alterados pelas correções receberam revisores independentes.
  Relatórios e JSON ficam em `/private/tmp/aip104-security-N3gcqA/`;
  limites de isolamento e planos sintéticos estão em `NEEDS-VALIDATION.md`.

- Autoreview P3 inicial identificou um P2 novo: cancelar um waiter duplicado
  cancelava a atualização de foreground pertencente a outro chamador. Regressão
  falhou 1/9 antes da correção; cancelamento agora pertence apenas ao criador
  da tarefa. Suíte completa iOS 27 após a correção: 682/682, bundle validado.
- UI final integrada antes dessa alteração isolada de lifecycle: 23/23 no
  iPhone/iOS 27, bundle validado. O ramo de lifecycle é desativado em UI testing;
  sua regressão é exercitada pelos testes unitários, não por esse override.
- Preflight final: exit 0, zero avisos. Metadados Xcode e os seis produtos
  Debug/Release (app, watch, widgets) confirmam 1.0.4 (60).

## Candidato local revisado

- Commit de implementação: `bc0542be64a79e996dfc9048b9cc234feb5cea6a`.
- Após corrigir o P2, iOS 26.5 também passou em 682/682. Release arm64 e
  análise estática foram repetidos e passaram (`release-review-fixed.log`,
  `analyze-review-fixed.log`). Watch `arm64_32` passou antes; nenhum código
  compartilhado/watch mudou depois desse gate.
- Autoreview P3 corretivo: `scoped-clean`, exit 0, nenhum achado acionável.
  Resultados fora do repositório: `/private/tmp/aip104-autoreview-rerun.json`.
  O hook passou sobre o índice exato e novamente ao criar o commit.
- UI: 23/23 no iPhone/iOS 27, iPhone/iOS 26.5 e iPad/iOS 27, sem falhas/skips.
  Inspeção de capturas sintéticas não resolve a limitação da árvore do Argent;
  autorização para esse método alternativo foi solicitada e segue pendente.
- Revisão Standards/Spec dos commits locais e CI/CodeQL do SHA remoto ainda
  pendentes nesta entrada. Nenhum push, tag, upload ou publicação foi realizado.

## Revisão dos commits locais

- Standards sobre baseline...`8d0f12b`: um P3 documentado (quatro comentários de
  tradução ausentes) aceito e corrigido; uma observação heurística P3 sobre o
  adaptador de persistência do onboarding mantida como limitação de teste.
  O adaptador explicita a decisão e preserva a regressão existente; seus testes
  não simulam a interface inteira nem um erro real de SwiftData. Os três
  chamadores foram inspecionados e guardam a conclusão após falha.
- Spec: nenhuma divergência funcional ou ampliação de escopo confirmada; uma
  lacuna de aceitação, a substituição ainda não autorizada da QA Argent.
  Push e CI/CodeQL seguem como próxima fase, não como resultados obtidos.
