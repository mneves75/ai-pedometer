# 2026-03-25

## Journal

- 00:32:57 -03: Bootstrapped repo-local operator docs and memory system because the repo did not yet contain `AGENTS.md`, `CLAUDE.md`, `MEMORY.md`, `memory/`, or `FOR_YOU_KNOW.md`.
- 00:32:57 -03: Captured standing rules from the user: reproducer-first bug handling, subagent-assisted bug fixing after the reproducer exists, figure-it-out operator posture, shell anti-buffering rules, Apple platform documentation path, and write-through memory expectations.
- 00:32:57 -03: Confirmed repo root with `pwd` and `git rev-parse --show-toplevel` before edits.
- 00:32:57 -03: Gathered project context from `README.md`, `TECH_STACK.md`, `PRD.md`, `APP_FLOW.md`, `project.yml`, and key source files under `AIPedometer/`, `Shared/`, `AIPedometerWatch/`, and `AIPedometerWidgets/`.
- 00:32:57 -03: Noted current doc drift worth preserving: `README.md` pointed to missing `AGENTS.md` and `CLAUDE.md`, and `PRD.md` no longer matches the premium subscription reality in the codebase.
- 00:34:59 -03: Verified the new operator docs and memory files exist, then fixed EOF formatting issues reported by `git diff --check`.
- 00:39:30 -03: Reconfirmed that scheme-level `xcodebuild test` was blocked by embedded watch-target resolution before tests started. Initial error said watchOS 26.4 needed to be installed even though a watch runtime existed locally.
- 00:41:34 -03: Created and paired local watch simulator devices, which proved that pairing alone did not fix the build gate.
- 00:48:54 -03: Confirmed device-install path to `iMarcus` failed for the same root cause as simulator builds, so the blocker was Xcode/watch toolchain state rather than the phone.
- 01:02:24 -03: After importing the matching watchOS 26.4 runtime bundle (`23T239`) and recreating the watch simulator on that runtime, the scheme began compiling again.
- 01:05:53 -03: Hit project-generation drift after adding new Swift files; had to run `xcodegen generate` and `bash Scripts/restore-entitlements.sh` before the new sources existed to Xcode.
- 01:08:49 -03: Mistake: first targeted test reruns burned time on warning-as-error issues in the new fake RevenueCat test client (`unused get()`/missing explicit returns). Fix was to make the fake client warning-clean before trusting test results.
- 01:15:16 -03: Full unit bundle passed after fixing the only failing regression expectation in `PremiumAccessStoreTests`.
- 01:20:29 -03: Canonical simulator E2E run passed with watch + widgets enabled and screenshots disabled: `output/e2e-post-fix/summary.md` reports 395 unit tests passed and 14 UI tests passed.
- 01:22:08 -03: Retried `bash Scripts/install-on-device.sh --device-name iMarcus --launch ...` after the watch-runtime repair; build, install, and launch all succeeded, and the script confirmed `OK: instalado em iMarcus`.
- 01:43:16 -03: RevenueCat integration upgraded from custom-only UI to official `RevenueCatUI` paywall + Customer Center presentation, with local test-store API key wired in `Config/Local.xcconfig`.
- 01:43:16 -03: Post-RevenueCat rerun passed again: `output/e2e-revenuecat-rerun/summary.md` reports 395 unit tests passed and 14 UI tests passed, and `iMarcus` install/launch succeeded with the new dependency path.
