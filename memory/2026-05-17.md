# 2026-05-17

## Session notes

- User requested: commit, push, and install on physical device `iMarcus`.
- Repo scope confirmed with `pwd` and `git rev-parse --show-toplevel`: `/Users/mneves/dev/PROJETOS_MOBILE/ai-pedometer`.
- `memory/2026-05-17.md` did not exist at session start and was created to satisfy the repo startup contract.
- Preflight before install: `git remote -v` showed `origin` as `https://github.com/mneves75/ai-pedometer.git`; `xcodebuild -showdestinations -scheme AIPedometer` listed physical destination `iMarcus`; `xcrun devicectl list devices` showed `iMarcus` connected; `xcrun devicectl device info lockState --device iMarcus` showed `unlockedSinceBoot: true`.
- Physical-device install proof: `DEVELOPMENT_TEAM=<LOCAL_DEVELOPMENT_TEAM_ID> bash Scripts/install-on-device.sh --device-name iMarcus --launch` built Debug successfully, installed `com.mneves.aipedometer` on `iMarcus`, launched the app, and the script's installed-app verification ended with `OK: instalado em iMarcus`.
- Build/install notes: signing used local Apple Development identity and provisioning profiles from the host. The build emitted the known benign AppIntents metadata warning for targets without AppIntents and an iOS validation warning about interface orientations; neither blocked build, install, or launch.
- Final local checks before commit: `bash Scripts/check-agents-sync.sh` passed, `bash Scripts/verify-device-identifiers.sh` passed, and `git diff --check` passed. `xcrun devicectl device info apps --device iMarcus --bundle-id com.mneves.aipedometer` confirmed AI Pedometer version `0.76` build `32` installed on `iMarcus`.
