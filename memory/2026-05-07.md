# 2026-05-07

## Session notes

- Started agent-surface sync for the new RevenueCat + Apple payments setup runbook, updating `CLAUDE.md` and `AGENTS.md` so future monetization work routes through the durable docs.
- Audited premium access gating and found that `PremiumAccessStore` could unlock AI premium for a single unrelated active RevenueCat entitlement or a historical known premium product. Locked both cases with regressions, then restricted premium restoration to configured or known premium entitlement aliases and active subscription product IDs only.
- Follow-up 2026+ best-practice pass checked current RevenueCat Trusted Entitlements guidance. `PremiumAccessStore` now explicitly enables entitlement verification in informational mode and fails closed when `CustomerInfo.entitlements.verification == .failed`.
- Full unit tests and analyzer passed after the RevenueCat hardening. An initial `Scripts/e2e-simulator.sh` attempt hit a transient iOS Simulator launcher failure (`NSMachErrorDomain Code: -308`, server died), and the rerun completed successfully with unit tests, UI tests, widgets, watchOS build, logs, and screenshots under `output/e2e-2026-05-07-150611/`.
- Reviewed `https://pedometer.app/` for comparable missing product surface and implemented the first concrete gap as Premium Expedition Mode: Workouts now exposes a gated toggle, and active sessions reduce live metrics cadence only after the controller rechecks the premium-gated persisted flag.
- Continued the Pedometer++ gap pass with a local-first Routes & GPX increment: Workouts can now import a GPX file for a persisted route summary, distance/elevation/waypoint stats, estimated time, and local preview while keeping full live/offline maps explicitly out of claim scope.
- Added the next site-parity slice for heart-rate data: HealthKit authorization now includes heart rate, `HealthKitService` can fetch the latest current-day BPM sample, `StepTrackingService` exposes it, and Dashboard renders a localized heart-rate card.
- Upgraded the GPX route preview to a MapKit-based preview with route polyline and localized start/finish markers, while keeping offline maps and Watch maps as explicit remaining gaps.
