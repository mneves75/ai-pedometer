# 2026-09-23 — revisão completa, auditoria de segurança e 1.0.6 (63)

## Pedido e critérios

Revisão Standards/Spec do ciclo 1.0.5 (`8d0f12b..HEAD`), revisão sênior do app inteiro, auditoria de
segurança completa antes de produção, `mneves-verify`, bump, docs, commit, push, TestFlight com tag
`-beta<N>` e produção quando possível. Critérios congelados antes das correções em
`agent_planning/2026-09-23-review-security-ship/GOAL.md`.

## Achados e destino

- Spec: faltavam testes para o botão Tentar Novamente e para `premium.offering_empty`. O segundo virou
  o seam `PremiumAccessStore.emptyOfferingCode(for:)` com teste (o `Offerings` da RevenueCat não tem
  init público). O botão só chama `refresh()`, já coberto; não há UI test determinístico para ele sem
  chave e sem rede. Duas frases exageradas do diff de docs foram corrigidas.
- Standards: `.black` cru em 14 lugares virou o token `DesignTokens.Colors.onAccent` (mesmo valor);
  DESIGN_SYSTEM.md desatualizado foi corrigido. Refutado: a entrada de acessibilidade em `[1.0.4]` do
  CHANGELOG está certa, porque `d95a836` ainda era 1.0.4 (60) no `project.yml`.
- Revisão sênior, confirmados no código: paywall sem os avisos de renovação automática, sem Termos de
  Uso (EULA) e sem Política de Privacidade (Guideline 3.1.2); Política de Privacidade e Feedback em
  `aipedometer.app`, domínio que não resolve (`dig` vazio); Pedir Compra (Ask to Buy) recusado travava
  os botões de plano para sempre; Restaurar Compras sem retorno; falha de compra dizia "assinaturas
  indisponíveis"; widget semanal "1 dias"; watch logava passos. Parcialmente refutado: comprar Premium
  sem Apple Intelligence ainda dá rotas GPX e Modo Expedição, então o paywall não é bloqueado, apenas
  informa o requisito.
- Segurança (padrão `standard`, 15 unidades, 3 ondas e crítico final limpo, relatório fora do repo):
  nenhuma vulnerabilidade confirmada. Uma pista `needs_validation`: `Int(Double)` sem guarda sobre
  somas do HealthKit, que qualquer app com permissão de escrita alimenta. O teste reproduziu o trap
  (`Fatal error: Double value cannot be converted to Int`) antes do `HealthCount.clamped`. Notas de
  hardening aplicadas: contagem O(n²) no parser GPX, `pages.yml` só publica `master`, `fatalError` sem
  a descrição do erro, botão Abrir Ajustes do AI Coach engolido pela política de links (bug real),
  snapshot do CoreMotion de um segmento pausado somado de novo depois de retomar (bug real).

## Evidência (Xcode 27.0 27A266a, simulador dedicado iPhone 17 Pro iOS 27.0)

- Vermelho antes da correção: HealthCount (trap), Ask to Buy (2 expectativas), Motion (snapshot
  antigo aceito). Verde depois.
- Unit `AIPedometerTests` 697/697, zero skips, xcresult validado.
- Watch `arm64_32` Debug: `BUILD SUCCEEDED`. `actionlint` OK.
- Host saturado durante toda a sessão (load 90–150 em 18 núcleos, 24 sessões).

## Verificação independente (mneves-verify)

- O `codex-auto` escolheu a conta pessoal, que recusou `gpt-6-sol` ("not supported when using
  Codex with a ChatGPT account"). O catálogo dessa conta lista `gpt-6-astra`, usado como verificador
  com override explícito (`-m gpt-6-astra`, somente leitura).
- Veredito FAIL em `4913e23`, com quatro achados reais: a "Política de Privacidade" era a seção de
  marketing, sem retenção, exclusão e revogação de consentimento (5.1.1(i)); `Int(distance)` em
  `StepTrackingService` ainda travava com valores finitos enormes; a expiração do Pedir Compra só
  rodava no init; o teste do widget não exercitava a composição. Também faltavam um teste para o
  botão Abrir Ajustes, evidência de preflight/pre-commit e um bundle de UI completo e limpo.
- Correção única: `docs/privacy.html` (en + pt-BR) como URL de privacidade do app e do ASC;
  `HealthCount.clamped(distance)`; expiração também no `refresh()`; `Localization.widgetStreakLabel`
  e `widgetWeeklySummary` testados; flag DEBUG `-force-ai-disabled` e UI test que exige o app Ajustes
  em primeiro plano. Links legais do paywall abrem direto no sistema, porque o sheet aberto a partir
  do AI Coach herdava a política de links dele.
- Vermelho antes de restaurar as correções: Pedir Compra no refresh (expectativa falhou), Abrir
  Ajustes (o app Ajustes não abriu, reproduzindo o bug original), distância (`Fatal error`).
  Verde depois: unit 702/702.

## Reverificação (única rodada) e fechamento

- Reverificação em `d860abc`: FAIL. Achados anteriores 2, 3 e 4 corrigidos; critérios 3 (gates) e
  4 (versão/docs/Git) PASS. Restaram imprecisões na política (IDFV que o SDK da RevenueCat envia em
  `X-Apple-Device-Identifier`, energia ativa, uso contínuo de movimento) e "iPhone" onde o app também
  roda em iPad; corrigidos depois da rodada, sem nova verificação (o contrato do mneves-verify
  encerra o ciclo). Critério 2 ficou BLOCKED: as asserções novas de restore, falha de compra e
  composição do widget exercitam APIs novas, sem execução vermelha registrada, e o reset de
  `openURL` dos links legais não tem teste (o paywall só abre com chave configurada, que o CI não
  tem). Unit 702/702 depois das correções de texto.
- Rótulo App Privacy: continua só "Purchases" (guia da RevenueCat: Device ID só com IDFA/integrações;
  User ID só com app user ID próprio).
- CI em `e6de263` falhou em 2 de 30 testes de UI no Xcode 26.3 hospedado: o novo teste de Abrir
  Ajustes (o runner não reportou o Ajustes em primeiro plano em 10 s) e a auditoria de
  acessibilidade do dashboard ("Potentially inaccessible text" ×3, a heurística intermitente de
  20/09). Os dois passaram localmente no iOS 26.5 e 27.0. O teste de Ajustes passou a exigir que o
  app saia do primeiro plano (30 s), que é o efeito que o bug impedia. Mudança só de teste; o build
  63 do TestFlight não muda.
- O segundo CI (`69fd7fd`) confirmou a auditoria de acessibilidade como intermitente (passou) e
  mostrou por que o teste de Ajustes falha: a gravação exibe a janela do app encolhendo (a transição
  começou), mas o runtime iOS 26.2 hospedado nunca termina de abrir o Ajustes. O teste de UI saiu;
  no lugar entrou a regra ast-grep `swift-ai-views-no-environment-openurl`, provada com violação
  plantada no próprio `AIAvailabilityBanner.swift` (exit 1) e árvore limpa (exit 0). A evidência
  vermelha original (`red2.xcresult`, iOS 27) continua valendo para o bug.

## Ficha da App Store a R$ 1,99 (store/app-store)

- Criado `store/app-store/` no formato `asc metadata`, como LUME e CaptureVault: app-info e versão
  1.0.6 em en-US/pt-BR, links de privacidade e suporte próprios do AIPedometer em
  conhecendotudo.com.br, e `pricing.json` com o price point BRA `p:10006` (R$ 1,99, proceeds
  R$ 1,48), lido via `asc pricing price-points`. `asc metadata validate --check-urls
  --subscription-app` limpo; o controle com subtítulo de 31 caracteres, descrição sem EULA e suporte
  na raiz do site falhou nos três pontos.
- Nada foi escrito no ASC: o app não tem price schedule nem availability, e o rascunho ainda é 1.0.4.
- A política do site (v1.0.0, 15/09) está mais fraca que a `docs/privacy.html` do repositório: não cita
  IDFV, IP e dados técnicos enviados à RevenueCat nem as estatísticas agregadas (finalidade Análises
  no App Privacy), e a seção 07 diz que o app "não coleta dados pessoais". O patch proposto para o
  site aguarda aprovação do dono (fora deste repositório).
- 1.0.7 (64): os links Privacidade/Suporte do app passaram a ser as páginas do site, por idioma
  (pt-BR → /pt/, o resto → /en/). `StoreListingTests` falhou antes da correção (4 falhas: github.io
  ≠ ficha) e passou depois; o controle plantado (preço 2.99, price point do LUME, subtítulo de 31)
  falhou nos três pontos. Revisões Standards/Spec: aceitos copy absoluto ("dados de saúde nunca saem
  do aparelho" → "nunca são enviados a nós nem a terceiros", pois Watch e iCloud sincronizam), volta
  da marketing URL do github.io, md sem cópia do JSON, README da ficha. Pesquisa: 5.1.1(i) exige que
  a política cite todos os dados; app pago + assinatura atrai escrutínio 3.1.2(a).
- Autoreview P3 do commit `596fb09` (Codex): P1 = política do site incompleta (é o gate já registrado;
  a correção é o patch do site, que aguarda o dono); P2 aceito = texto promocional e descrição diziam
  que os dados "ficam no aparelho", falso com o iCloud do Saúde; agora descrevem o que o app envia
  ("o app nunca envia seus dados de saúde a nós nem a terceiros"). Ao vivo (iPhone 17 Pro, iOS 27.0,
  headless): About > Privacy Policy gerou no log do SpringBoard "Handling OpenURL from AIPedometer:
  https://www.conhecendotudo.com.br/en/apps/privacy/aipedometer/"; o Safari não veio para a frente
  no simulador headless, mas abre a mesma URL via open-url.
- Verificação independente (Codex GPT-6, contexto novo): FAIL em 2 de 9 critérios. (1) Faltava no gate
  que a política do site diz "nenhum dado de saúde sai do aparelho" e "o Watch recebe por app group",
  mas `WatchSyncService` envia passos, meta, sequência e semana ao Watch pareado via WatchConnectivity:
  gate e patch do site corrigidos. (2) Push pendente: é o passo seguinte.
- Reverificação (GPT-6): 8/9 PASS; FAIL no README:193 ainda em 1.0.6 (63), corrigido depois, sem nova rodada (limite de uma correção). Patch do site: frase da idade passou a ressalvar as compras processadas pela Apple; `git apply --check` limpo no repositório do site (não aplicado; outra sessão tem edições não commitadas lá, fora de apps.ts).

## 1.0.8 (65): compra única de R$ 1,99, sem assinatura (decisão do dono)

- O dono escolheu a opção 2 (como LUME e CaptureVault): tudo liberado, RevenueCat removida. Worker Codex
  GPT-6 Sol removeu SDK, PremiumAccessStore, paywall, gates, flags, scripts/workflow RevenueCat e produtos
  de assinatura do .storekit; Tip Jar (consumível StoreKit 2) mantido. Revisão do diff achou regressão:
  o worker reagendava o lembrete inteligente a cada foreground (geração no modelo a cada vez) e cancelava
  com IA momentaneamente indisponível. Trocado por `legacySmartReminderAction` (.none/.clear/.resume):
  só um lembrete suspenso pela assinatura antiga volta, uma vez, quando o modelo está disponível; a chave
  fica até reagendar ou o usuário desligar. Teste vermelho (2 falhas no stub) e verde depois.
- Esquema `AIPedometer-Sandbox` restaurado (Tip Jar no sandbox real, em aparelho). Ficha 1.0.8: "Compra
  única, sem assinatura", sem EULA; `privacy.json` = Data Not Collected. StoreListingTests vermelho com o
  texto antigo (6 falhas) e verde com o novo. Unit 636/636 (Xcode 27.2 beta, iPhone 17 Pro iOS 27.0).
- Site: a seção do AIPedometer foi reescrita (nada coletado, Apple única destinatária, Notificações, Watch
  via WatchConnectivity) e a frase da Lente IA (pedido de ai-filters-f3) entra junto na v0.9.3, depois da
  manutenção do VPS por conhecendotudo-17; minutadraft-ff publicou a v18.
- ASC (autorizado: "do it all"): versão rascunho 1.0.4 → 1.0.8 (`d4745e9e…`); `asc metadata apply` com 8
  campos (URLs de privacidade/suporte, descrição, texto promocional; en-US e pt-BR) e releitura idêntica ao
  repositório; App Privacy "Data Not Collected" aplicado e publicado (removidas as duas declarações
  Purchase History da RevenueCat); price schedule BRA `p:10006` = R$ 1,99 (proceeds 1,48, `isFree:false`),
  sem erro de agreement; disponibilidade em 175 territórios + novos. As assinaturas no ASC não foram
  tocadas nem apagadas. Contato de revisão, idade, direitos de conteúdo e copyright seguem com o dono.
- Onboarding redesenhado (HIG Onboarding + Privacy/pre-alert): valor, metas com atalhos, "Conecte o app
  Saúde" com um único "Continuar". No simulador headless a folha do Saúde não aparece (cena hospedada
  realizada no log, mesma limitação do Safari); verificar no owner-iPhone.
- Auditoria de acessibilidade (Xcode 27, vidro de produção): o worker isentava contraste por rótulo em inglês
  e rolava com `swipeUp()`. Depuração com print do handler (id/label/frame) mostrou que auditorias de telas
  roladas marcam texto preto opaco em massa (≈10 falhas, com ou sem momentum), e que em repouso só falha
  "Training Plans", inteiro atrás da tab bar. Final (depois da verificação GPT-6 apontar redução do gate):
  Dashboard e Workouts auditados em repouso e de novo após um arrasto curto que segura antes de soltar
  (`scrollWithoutMomentum`, sem momentum): passa. Conteúdo inteiramente atrás da tab bar não é julgado em
  repouso (`isHiddenBehind`, teste unitário) e é auditado depois da rolagem. Única isenção nova: o rótulo
  do botão de vidro "Import GPX" (`routeImportButtonLabel`, preto sobre verde ~9,8:1), mesma classe do
  "Grant Access" já isento. Erro meu no caminho: `enum
  AIAvailability` duplicado em A11yID quebrou o build.
- Autoreview P2 aceitos: o worker tinha removido o marcador de suspensão usado quando a IA fica
  indisponível (lembrete cancelado sem volta) e passado a regenerar o lembrete a cada visita a Ajustes.
  Restaurado como `smartRemindersSuspended` (mesma chave gravada), limpo só após reagendar ou quando o
  usuário desliga; Ajustes só reagenda se suspenso.
- Entrega 1.0.8 (65): archive Xcode 27.0 de `e9eb05e`, validador ok, export com PATH do sistema, IPA
  `eae7362e…204e` (sem RevenueCat, get-task-allow false). Upload `aa7d8a9f` VALID/exempt, grupo Internal
  Testers. Tag `v1.0.8-beta1` → `e9eb05e`, local: `gh` foi deslogado na máquina no meio da sessão (push
  falhou com "could not read Username"). Release instalado e aberto no owner-iPhone. Reverificação GPT-6: FAIL
  (assinaturas no ASC, comportamento antigo de Ajustes, teste do filtro); o teste do filtro foi corrigido
  depois (`d696431`) e as suítes rodaram de novo no HEAD final: 637/637 e 30/30.
