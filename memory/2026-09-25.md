# 2026-09-25

- Envio da 1.0.8 à App Store. O dono e a sessão ai-pedometer-66 preencheram classificação etária, direitos de
  conteúdo, contato e notas da revisão, apagaram as assinaturas Premium nunca enviadas e criaram a gorjeta
  (IAP `6816210322`, R$ 9,90). Aqui: captura de revisão da gorjeta enviada (tela real de Suporte, "Loading
  price…" porque o simulador não carrega preço), nome pt-BR alinhado ao botão ("Me pague um café"), IAP
  `READY_TO_SUBMIT`, submissão `98a5302a` criada com a gorjeta.
- Ao adicionar a versão, a Apple recusou por dois itens que o `asc validate`/`review doctor` não mostram:
  capturas do Apple Watch obrigatórias (o app embute o relógio) e a declaração de dispositivo médico regulado.
  Capturas do relógio: o par do simulador não serve (o iPhone simulado conta 0 passos fora do modo de teste,
  e em `-ui-testing` a inicialização não liga o WatchConnectivity); seam `-watch-demo-payload` só para testes
  (Release rejeita, teste vermelho antes), capturas 416×496 en-US e pt-BR enviadas. Falta só a declaração
  médica (web 2FA do dono).
- Detalhe estético do relógio em inglês: "5.98km" sem espaço (pt-BR sai "5,98 km").
- Descrição pt-BR da gorjeta tinha 53 caracteres; a Ajuda do ASC limita a 45 (nome 2-30). Corrigida para
  "Gorjeta única para apoiar o desenvolvimento." (44): foi preciso tirar a versão da IAP da submissão
  (rascunho trava edição; a 1ª tentativa logo após remover ainda falhou, ~20 s depois passou) e readicionar.
  ai-pedometer-66 publicou `650b79d` (tip-jar.json/review.json testados). Notas de revisão corrigidas: o relógio
  não tem treino, só passos/distância/sequência. Codex com 401 nos dois perfis (auth.json mudou às 20:36).
- Estado: submissão `98a5302a` com a IAP; versão 1.0.8 (build 67) recusada só pela declaração de dispositivo
  médico regulado (dono, web 2FA). Próximo: dono declara → adicionar versão → enviar → tag `v1.0.8` na
  publicação.
- Dono declarou dispositivo médico. Versão 1.0.8 (67) adicionada e submissão `98a5302a` enviada em 2026-09-26T00:16Z: app e IAP `WAITING_FOR_REVIEW` (o `submissions-submit` estourou o tempo no cliente, mas o servidor aceitou; não reenviar). Tag `v1.0.8` fica para a publicação.
- Dono viu "Produto não disponível" na gorjeta no TestFlight às 00:17 UTC, 7 min após eu editar a descrição da
  IAP. Checado: IAP com preço, 2 localizações, 175 territórios (BRA incluso), captura; App ID com
  IN_APP_PURCHASE; ID do produto confere (StoreListingTests). TN3186: edição leva até 1 h no sandbox; sandbox
  é documentado só em aparelho (simulador também falhou, sem valor de prova). Sem mudança de código. Pendente:
  dono conferir Contratos/Impostos/Banco "Active" e tentar de novo após 01:10 UTC.
- Build 68: distâncias em inglês saíam "5.98km" (MeasurementFormatter `.short`); agora `.medium` ("5.98 km",
  "3.11 mi", "98 ft", "29.5 in"). Conferido em 9 regiões: só espaço e ft/in no lugar de ′ ″; pt-BR igual.
  Teste vermelho (FormattersTests) antes; unit 656/656, UI 31/31; autoreview GPT-6 Astra P3 limpo (0,91).
  Codex voltou a autenticar. 67 segue em revisão; 68 vai só ao TestFlight.
- mneves-verify do 68 (GPT-6 Astra, snapshot): FAIL só no critério 2 como escrito ("só espaço"): árabe passa a
  escrever a unidade por extenso ("مترًا"), pt-BR com medidas dos EUA abrevia ("mi", "pol."), coreano segue
  "5.98km" (padrão CLDR). Critérios 1, 3 (147 casos, 21 locais), 4 e 5 PASS. Mantido `.medium` global (melhor
  para es/ja/en); critério julgado estrito demais, sem nova rodada. Teste fixa pt-BR padrão sem mudança.
