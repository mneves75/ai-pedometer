# 2026-09-25

- Envio da 1.0.8 à App Store. O dono e a sessão ai-pedometer-66 preencheram classificação etária, direitos de
  conteúdo, contato e notas da revisão, apagaram as assinaturas Premium nunca enviadas e criaram a gorjeta
  (IAP `6816210322`, R$ 9,90). Aqui: captura de revisão da gorjeta enviada (tela real de Suporte, "Loading
  price…" porque o simulador não carrega preço), nome pt-BR alinhado ao botão ("Me pague um café"), IAP
  `READY_TO_SUBMIT`, submissão `98a5302a` criada com a gorjeta.
- Ao adicionar a versão, a Apple recusou por dois itens que o `asc validate`/`review doctor` não mostram:
  capturas do Apple Watch obrigatórias (o app embute o relógio) e a declaração de dispositivo médico regulado.
  Capturas do relógio: o par do simulador não serve (o iPhone simulado conta 0 passos fora do modo de teste,
  e em `-ui-testing` a inicialização não liga o WatchConnectivity); seam `-watch-demo-payload` só para testes
  (Release rejeita, teste vermelho antes), capturas 416×496 en-US e pt-BR enviadas. Falta só a declaração
  médica (web 2FA do dono).
- Detalhe estético do relógio em inglês: "5.98km" sem espaço (pt-BR sai "5,98 km").
