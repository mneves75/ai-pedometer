# 2026-09-15

- Pedido: revisão completa (pesquisa externa, code-review Standards+Spec desde `v0.96-beta1`, revisão de
  segurança pré-produção, autoreview, boas práticas contra a documentação do Xcode), corrigir tudo, bump,
  docs, commit, push e entrega (iMarcus; depois redirecionado para TestFlight com tag beta). Ledger de cada achado ao fim deste arquivo; plano vivo:
  `agent_planning/2026-09-15-full-review-and-ship.md`.
- **Achado mais grave, invisível a nove auditorias:** `Info.plist` nunca teve `NSSupportsLiveActivities`,
  então `areActivitiesEnabled` era falso e nenhum treino jamais mostrou Live Activity. Corrigido com teste
  no plist-fonte. Com a feature viva pela primeira vez, o ciclo de vida foi corrigido (fim com métricas
  finais, órfãos e descarte `.immediate`, varredura de órfãos não encerra a atividade nova).
- Segurança (fonte, sem sandbox de SO para executar alvo): um único achado confirmado, baixo —
  `APP_STORE_ID` do ambiente de lançamento honrado em Release. Corrigido. Nenhum médio/alto/crítico.
  Needs-validation externos registrados no ledger (mapeamento de entitlements no RevenueCat, proteção de
  dados/backup em aparelho, limites do XMLParser, permissões efetivas do token do GitHub).
- **Erro meu de orquestração:** despachei três subagentes Claude em paralelo; os três pararam ~02:55 no limite
  de sessão da conta com edições parciais em 87 arquivos. O que salvou foi a regra de teste-vermelho-primeiro:
  a árvore compilava e os testes vermelhos descreviam o trabalho restante. Build da árvore parcial: 647 testes,
  60 issues, todos nos testes novos. Terminei os três lotes revisando cada hunk. Lição no FOR_YOU_KNOW.
- Refutado por fonte: rótulo "Steps" do treino ativo deveria seguir o modo (métrica de treino é CMPedometer
  sempre); o subagente havia implementado a mudança e eu removi.
- Recusados com motivo: agrupar cartões glass em `GlassEffectContainer` (funde cartões separados, sem medida
  de custo), `alert(item:)` (muda dismiss), persistir modo em planos salvos (migração SwiftData).
- A primeira versão da busca de meta por dia usava o último instante do dia e quebrou três testes existentes:
  fixtures (e lojas antigas) têm lacuna de 1 s na troca de meta à meia-noite. Trocado para sobreposição
  (meta de início mais recente ativa em algum momento do dia). Os testes antigos estavam certos.
- Mutação para os consertos que não tiveram vermelho observado: neutralizados, os 5 testes (APP_STORE_ID,
  URL https, nome GPX por arquivo e legado, elevação) falharam; restaurados. A primeira mutação não compilou
  (warnings-as-errors em código inalcançável) e o run seguinte usou binário velho — resultado descartado.
- Disco do host caiu de 34 GiB para 0,7 GiB por builds de outras sessões; `xcodebuild clean` nos DerivedData
  dos subagentes mortos devolveu ~3 GiB (o guard bloqueia `rm -rf`, não contornei).
- Versão passa a três componentes (formato documentado de `CFBundleShortVersionString`): 1.0.1 (57).
  Bloqueio de produção do RevenueCat `test_` inalterado.

## Ledger de achados (cópia rastreada do plano em agent_planning/, que é ignorado pelo Git)

Legend: FIXED (red→green observed) · FIXED* (no seam; build/device evidence) · REFUTED · DECLINED (reason) · DEFERRED (external blocker) · W1/W2/W3 = delegated worker, verify diff.

### Tooling / CI / docs (lead)
- autoreview P2 selector errexit abort on unusable candidate → FIXED (`Scripts/tests/xcode-toolchain.sh` strict-mode case red→green).
- autoreview P2 preflight loses DEVELOPER_DIR from `$(...)` → FIXED (bogus DEVELOPER_DIR: build-settings gate FAIL→OK).
- Standards H2/H3 tests + preflight gates ran PATH bash, not /bin/bash 3.2 → FIXED (all Scripts/tests + preflight gates use /bin/bash).
- Standards H1 watch PrivacyInfo 1C8F.1 (App Group reason) without app group → FIXED to CA92.1 (Apple reason JSON via research).
- J1 empty majors array unbound under bash 3.2 → FIXED guard. J2 vacuous negative-count test → FIXED (mutation removing clause now fails). J4 dead env vars → FIXED.
- Spec 4 build-settings gate accepted names merely referenced (SWIFT_FLAGS ⊂ OTHER_SWIFT_FLAGS), used xcode-select, allowlisted 4 names → FIXED (definitions only, selector, allowlist removed; planted SWIFT_FLAGS red→green).
- Tooling 4 staleness validator failures ignored under caller `set +e` → FIXED (malformed SHA fixture red→green).
- Tooling 3 device-id scan case-sensitive → FIXED `-i` (lowercase planted red→green; ordinary hashes pass; repo clean).
- Tooling 8 entitlements gate missed forbidden keys → FIXED (widget HealthKit, ungated hardened-process red→green; opt-in honored).
- Tooling 5 `test-payments-device.sh` tester add failed silently → FIXED (named exit 6, publish not reached; red→green). Authority claim REFUTED: script's purpose is publishing; invites only with explicit TESTFLIGHT_TESTER_EMAILS.
- Tooling 2/9/10 CI picks beta Xcode; no persist-credentials:false; artifact retention; push runs cancellable; ast-grep rule tests never run → FIXED (actionlint clean; ast-grep test planted failure exit 4).
- Tooling 11 e2e screenshot whitespace regex literal backslash → FIXED.
- Spec 5 docs overstated preflight coverage / generic destination / selector precedence → FIXED docs.
- Tooling 12 project-structure.md App Store ID claim → REFUTED (`Config/Local.xcconfig.example:4` sets it).
- Standards J5 rename check-agents-sync.sh → DECLINED (churn across CI/hooks/docs for a name; behavior correct).
- Standards J6 duplicated Python else branches in e2e-simulator.sh → DECLINED (defensive at the Python boundary, zero cost).
- Spec 6 personal user-agent in staleness script → DECLINED (global contract mandates that UA for HTTP downloads).
- Spec 7 / Standards Wrangler devDependency unused → DECLINED pending owner: purpose unknown, removal not a fix; reported.
- CodeQL `actions` language → DECLINED (cost/untestable locally; actionlint covers syntax).

### Security (audit: recon ×2, Codex hunter, lead verification; source-only, no OS sandbox)
- CONFIRMED low `launch-config:app-store-id-release-env` → FIXED (`allowsEnvironmentOverrides`, test; red observed by mutation).
- NEEDS_VALIDATION `premium:management-url-unvalidated` → hardened https-only + host (parameterized test; red observed by mutation).
- NEEDS_VALIDATION widget destructive decode → DECLINED change (documented self-heal, test encodes it; app rewrites snapshot).
- NEEDS_VALIDATION entitlement alias fallback → DEFERRED (RevenueCat dashboard mapping is external).
- NEEDS_VALIDATION GPX aggregate internal-entity amplification → DEFERRED (existing per-element cap test; XMLParser limits are runtime).
- NEEDS_VALIDATION data protection/backups → DEFERRED (physical device inspection).
- NEEDS_VALIDATION CodeQL PR write token → REFUTED in practice (fork PRs get read-only token; sole collaborator) — owner setting.
- Rejected with source: tool days (self-impact; clamped anyway as hardening by W2), chat length, watch far-future date, logging, link paths, CI cache/artifacts.

### App code
Workers W1–W3 were Claude subagents; all three stopped at ~02:55 on the account session limit (resets
06:20) with partial edits. The lead reviewed every hunk and finished the work. Red evidence: a build of the
partial tree ran 647 unit tests with 60 issues, all in the new regression tests listed as "red observed".

- Tooling P1 `NSSupportsLiveActivities` missing (Live Activities never started in any build) → FIXED, red observed (`infoPlistSupportsLiveActivities`).
- Shared P1 Live Activity ended with zeros; P2 orphan cleanup race → FIXED (W1: pure `endRequests` policy with tests; latest state kept, orphans `.immediate`, discard `.immediate`; cleanup snapshot excludes current; start awaits cleanup). Red not observed by lead (W1 fix was in the partial build).
- Shared P2 WCSession pre-activation send dropped → FIXED* (pending snapshot flushed on `.activated`; no WatchConnectivity seam).
- Watch ignored `receivedApplicationContext` on relaunch → FIXED* (handled on activation; acceptance state still orders it).
- Shared P2 widget snapshot showed yesterday/sample data → FIXED (`SharedStepData.widgetSnapshotData` + tests; red not observed by lead).
- Shared P1 wheelchair pushes shown as steps on watch/widgets → FIXED (optional `activityMode` on both payloads, `try?` decode for unknown future modes, labels/icons/distance, snapshot flushes on mode change, decode tests).
- Shared P3 GPX elevation overflow → FIXED (plausible band; test; red observed by mutation).
- Shared P2 context-vs-queued clock skew → DECLINED (needs skew + reinstall; ordering tests encode current rules).
- Core P1 coach session rebuilt on every activation → FIXED, red observed (reuse live session; replacement clears visible conversation).
- Core P1 mid-day goal change judged by previous goal → FIXED, red observed (`goal(forDayContaining:calendar:)` requirement: latest-starting goal overlapping the day, robust to legacy boundary gaps; used by all 7 daily consumers incl. StreakCalculator, tool, sync, insight, plans; three pre-existing midnight-change tests still pass).
- Core P2 cancellation cached a fallback → FIXED, red observed (fallback throws CancellationError distinctly; InsightService never caches or reports errors for a cancelled caller).
- Core P2 HealthKit query continuations ignored cancellation → FIXED, red observed (`HealthKitQueryBridge`, stop + exactly-once resume; heart-rate and sum queries; descriptor-based collection query already cancellable).
- Core P2 fetch failure treated as emptiness → FIXED, red observed (setGoal refuses; badge cache stays unloaded and unlock refuses).
- FM iOS 27 `LanguageModelError` family mapped to generic failure → FIXED, red observed; guarded by `#if compiler(>=6.3)` so CI's Xcode 26.3 still builds.
- Hardening: tool `days` clamped 1...90 → FIXED, red observed.
- AI caches keyed by activity mode (daily insight, workout recommendation) → FIXED, red observed; view trigger includes mode (W3).
- Features P1 Settings concurrent recovery cancelled a newer owner's reminder → FIXED, red observed (`resumeSuspendedSmartReminder` defers to a newer scheduling owner, clears on current failure; premium/suspension policy unchanged).
- Features P2 History stale load published over newer load → FIXED, red observed (`HistoryAnalysisGate.load` ownership).
- Features P2 iPad single stack kept pushed detail across sidebar items → FIXED* (`.id(selectedTab)`; visual check pending).
- Features P3 previews missing environment → FIXED (Debug only, compile-checked).
- Features P3 plurals "1 days"/"1 waypoints" → FIXED, red observed (catalog plural variations en/pt-BR; completeness test accepts variations; interpolated literals skipped by the scan, asserted in PluralTests).
- Features P3 soft-deprecated toolbar APIs → FIXED (toolbarVisibility/toolbarBackgroundVisibility, iOS 18+). `alert(item:)` → DECLINED (dismissal semantics would change; soft deprecation only).
- Features P3 raw colors → FIXED (tokens `pink`, `brown`, `neutral`).
- Features P2 GlassEffectContainer grouping → DECLINED (separate cards within container spacing blend into one shape; no measured perf problem; AGENTS requires a baseline for perf claims).
- Features claim ActiveWorkoutView "Steps" should follow activity mode → REFUTED (workout metrics are CMPedometer steps in every mode; `WorkoutLiveMetricsSource`).
- Saved training plans persisting their mode → DECLINED (SwiftData schema migration; product decision).
- Spec 1/3 GPX name bound gaps and loose test → FIXED (literal 200; tests; red observed by mutation).
- Standards J3 canonical-equivalence compare → FIXED. J7/J8 dead aliases/history comments → FIXED.
- Open item: 14 vacuous localization assertions → 7 key-list assertions now check catalog membership (+ negative control); 7 enum display-name smoke checks kept (literal-scan test is the real guard).

### Rodada 2 — revisão do commit `c626937` (autoreview P3 em clone congelado + Standards/Spec via Codex)
Todos confirmados na fonte; cada um com teste observado vermelho antes do conserto.
- P1 `#Preview` do MainTabView chamava helper `#if DEBUG`: build Release (archive) não compilava; CI só compila
  Debug → FIXED (previews dentro do mesmo `#if DEBUG`; compilação Release verificada).
- P2 lembrete: tentativa antiga terminando depois de a mais nova falhar ainda "deferia" e deixava lembrete agendado
  com a UI em suspenso → FIXED (`newestOwnerFinished`), vermelho observado.
- P2 badge: `existingBadgeTypes` vazio de leitura falha sobrepunha leitura recuperada e duplicava → FIXED (união),
  vermelho observado.
- P2 bridge HealthKit: cancelamento entre guardar a continuação e `execute()` parava uma query ainda não iniciada →
  FIXED (estado `.cancelled` distinto; stop pós-execute), vermelho observado por mutação.
- Spec: troca de modo não furava throttles de watch e widget → FIXED (modo nas decisões puras), vermelho observado.
- Spec: widget semanal ainda dizia "steps" → FIXED (cabeçalho e acessibilidade por modo; 3 chaves novas en/pt-BR).
- Spec: Live Activity terminava com a última atualização throttled, não a final → FIXED, vermelho observado.
- Spec: watch/widgets ainda "1 days" (`String(format:)` recebe a forma `other`) → FIXED (`Localization.streakDays`
  interpolado), vermelho observado.
- Spec: totais opcionais do HealthKit engoliam `CancellationError` → FIXED, vermelho observado.
- Standards: URL de gerenciamento sem allowlist de host (SECURITY-GUIDELINES) → FIXED (`apps.apple.com`), vermelho
  observado.
- Standards: formatter de acessibilidade duplicado → FIXED (`ActivityTrackingMode.progressAccessibilityValue`).
  Tuplas de cache (data clump) → DECLINED (privadas e locais ao serviço).
- Spec: CHANGELOG exagerava "cancelamento distinto em toda a IA" → corrigido para o comportamento real.
- UI: `testTrainingPlansOpensFromWorkouts` falha no simulador iPhone 17 Pro Max iOS 27.0 também em `08c0b72`
  (mesma linha `AppDriver.swift:180`) e passa no iPhone 17 Pro; suíte UI completa no 17 Pro: 20/20.
- Entrega: pedido redirecionado para TestFlight + tag `-beta`. Bloqueado: `Config/Local.xcconfig` só tem chave
  RevenueCat `test_` (verificado por prefixo); archive Release sairia sem premium e o validador rejeita. Sem upload
  e sem tag beta até existir chave `appl_`.
- Autoreview rerun (única rodada extra) sobre `4014fdb`: um P2 real — `onCancel` chamava `stop()` antes de
  gravar `.cancelled`, e com preempção a checagem pós-execute ainda via `.waiting`. FIXED* (grava antes de
  parar; sem seam determinístico para preempção entre instruções). Unit 654/654 depois do conserto. Gates finais
  na árvore: unit 654/654, UI 20/20 (iPhone 17 Pro), analyze limpo, watch arm64_32 ok, Release para iOS
  (arm64, sem assinatura) compila com 1.0.1 (57) e `NSSupportsLiveActivities` no plist embutido. Build Release
  para simulador falha com RevenueCat "incompatible target" por ser multi-arquitetura (mesma causa do destino
  genérico documentada); não é defeito do app.
- Continuação: `testTrainingPlansOpensFromWorkouts` no simulador iPhone 17 Pro Max — `AppDriver.tap(id:)` só
  tocava elemento com centro dentro do frame ou `isHittable`, e repetia sem rolar. Com rolagem para o elemento
  fora da tela, o teste passou (o log mostra um swipe para cima: o cartão estava abaixo da área visível, não acima
  como supus primeiro). Suíte UI completa no Pro Max: 20/20. RevenueCat continua `test_`; TestFlight segue bloqueado.


## 1.0.2: distâncias seguem a região do aparelho
- Causa: formatters sem `locale` explícito + override `.environment(\.locale, en-US/pt-BR)` no app.
  Com locale en-US, 500 m renderizava "546.807yd" (confirmado via `swift /tmp/fmt.swift`); pt-BR renderizava "500 m".
- Fix: `locale = .autoupdatingCurrent` em Number/Measurement/RelativeDate formatters
  (`Formatters.swift`, widget Live Activity, Dashboard) + `Localization.format` com
  `Locale.autoupdatingCurrent`; removido o override de `.locale` no `AIPedometerApp`
  (tradução continua via `L10n`/bundle; nenhuma `Text("literal")` dependia do env).
- Versão 1.0.2 (58): `project.yml` → `xcodegen generate` (1.0.2/58 confirmado) → CHANGELOG + README.
  Commit `6b5d852`, push master, tag `v1.0.2-beta1` (push). Preflight OK; unit focados OK; pre-commit OK.
- iMarcus: Debug 1.0.2 (58) instalado e com launch OK (`devicectl` confirma 1.0.2/58).
- TestFlight NÃO feito: `Config/Local.xcconfig` ainda só tem chave RevenueCat `test_` — upload Release
  sairia sem premium e o validador rejeita. Mesma pendência documentada desde 1.0; precisa de chave `appl_`.

## 1.0.3: revisão do 1.0.2 (regionais) + revisão de segurança
- Achado principal: o 1.0.2 não resolvia o iMarcus (`Language en-US`, `Locale en_US` via lockdown). No simulador iOS 27
  configurado pelo app Ajustes (Região EUA, Sistema de Medida Métrico, Formato Numérico 1.234.567,89), HEAD `a0ee642`
  mostrava "3,788mi" e "8.000 of 10,000 steps" (`.scratch/locale-1.0.3/red-head-a0ee642-dashboard-enUS-metric.png`).
  A causa do 1.0.2 ("formatters sem locale") era falsa: `MeasurementFormatter()` já usava `Locale.current`; o teste da
  sessão anterior usou `Locale(identifier:)` explícito, que não reproduz preferência fora do identificador.
- Fatos medidos: `MeasurementFormatter` ignora Sistema de Medida mas respeita Formato Numérico; `FormatStyle` e
  `UnitLength(forLocale:usage:)` respeitam Sistema de Medida e `FormatStyle` ignora Formato Numérico; `%lld` não agrupa.
  Custo: ~0,1 ms por distância (formatter novo por chamada, maior parte na busca da unidade).
- Conserto: unidade por `UnitLength(forLocale:usage:)` (.road/.general/.person) + `MeasurementFormatter` `.providedUnit`;
  contagens por `formattedSteps` (anel, metas, onboarding, widgets, planos, card IA, badges, fallback semanal, a11y);
  Live Activity e prompts de IA (km fixo) pelo mesmo formatter; badges sem "5,000"/"kilometers" fixos (campos
  `title`/`description` de `BadgeDefinition` nunca eram lidos e saíram). 3 chaves `%lld` viraram `%@` + 2 chaves de badge.
- Achado extra: o anel do Dashboard não expunha elemento de acessibilidade (VoiceOver pulava o progresso). Visto no
  anexo "App UI hierarchy" do XCUITest; consertado com `.accessibilityElement(children: .ignore)`.
- Vermelho observado antes de cada conserto: unit `distanceFollowsTheDeviceMeasurementPreference` ("3,107mi") e
  `progressAccessibilityValueFormatsCountsLikeTheVisibleRing` no simulador configurado; UI
  `testDashboardDistanceFollowsMeasurementSystemPreference` ("Distance: 3,788mi") e
  `testDashboardCountsFollowNumberFormatPreference` ("8,000 of 10,000 steps") com mutação temporária do formatter do
  1.0.2 (a 1ª rodada falhou só por não achar o anel — não contada como vermelho). `-AppleLocale/-AppleMetricUnits/
  -AppleMeasurementUnits/-AppleICUNumberSymbols` reproduzem o estado do Ajustes (provado com binário no simulador).
- Segurança (Codex, só leitura, HEAD `a0ee642`): 0 vulnerabilidades confirmadas; correções do 1.0.1 seguem válidas.
  Notas: links de IA agora só HTTPS (teste vermelho antes); GPX "billion laughs" real rejeitado pelo parser (erro 111,
  1 ms) e agora testado. Recusadas com motivo (FOR_YOU_KNOW, non-findings): limites de payload watch/App Group, classe
  de proteção do SwiftData, limites de entrada/saída do coach.
- Code review Matt Pocock (Codex): Standards sem violação documentada; aplicados limite 999,4/999,5 m, texto do
  CHANGELOG e helper duplicado da Live Activity; recusado renomear `formattedSteps` (estado todo usa "steps"). Spec:
  aplicados `%lld`, fallback da IA e badges; recusada "remoção do env locale é scope creep" (é o que permite a região).
- RevenueCat: staleness exit 10 (5.81.1 fixado, 5.89.0 disponível). Decisão: manter no 1.0.3 — atualizar SDK de
  pagamento exige compra/restauração em sandbox e novo archive, impossíveis sem a chave `appl_`.
- Gates finais (simulador iPhone 17 Pro iOS 27.0 restaurado para en_BR; Xcode 27.0 27A266a): unit 661/661, UI 22/22,
  analyze ok, watch arm64_32 ok, Release arm64 sem assinatura compila. Antes, no estado EUA+Métrico+1.234,56: unit
  659/659 e UI 20/20. Shell tests, shellcheck, actionlint, pnpm audit e invariantes ok.
- Simulador: região voltou para Brasil; idioma principal virou `en-BR` (antes `en-US`) porque o iOS reescreve na troca.
- Commit `de05cc6` (1.0.3 (59)). Autoreview P3 (Codex gpt-6-astra, `--mode branch --base 6d57fe3`): scoped-clean, 0
  achados. 1ª tentativa falhou em 0 s porque `--prompt-file` exige caminho relativo ao repo — usar `--prompt`.
- iMarcus: Debug 1.0.3 (59) instalado e com launch OK (`devicectl` confirma 1.0.3/59). Verificação visual das unidades
  no aparelho não feita (tela mostra dados de saúde reais); fica como checagem física do dono.
- TestFlight e tag beta NÃO feitos: `Config/Local.xcconfig` segue com chave RevenueCat `test_`; sem token de service
  account do 1Password neste host, e ler o cofre pessoal exige consentimento explícito.
- Push `a0ee642..b86b102`: CI (Build & Test, ~40 min no runner) e CodeQL verdes em `b86b102`; análise CodeQL publicada
  para o SHA com 27 regras e 0 alertas abertos.
- Pendências para a próxima entrega: chave RevenueCat `appl_` (item do 1Password a indicar pelo dono) → archive Release,
  validação, upload TestFlight e tag `v1.0.3-beta1`; olhar o cartão Distância no iMarcus (espera-se km/m se o
  Sistema de Medida estiver em Métrico); decidir atualização RevenueCat 5.81.1 → 5.89.0 com teste de compra em sandbox.
