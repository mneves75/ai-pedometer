# 2026-05-06

## Session notes

- Started physical-device install flow for `iMarcus`.
- Built, installed, and verified the Debug iOS app on physical device `iMarcus` with `Scripts/install-on-device.sh`; the launch-only attempt failed while the device was locked, then a no-launch install/verify pass completed successfully.
- Added a dedicated RevenueCat + Apple payments setup runbook under `docs/revenuecat/apple-payments-setup.md` and linked it from README and the RevenueCat guide.
