# 2026-05-31

## Goal-mode full-stack audit cycle → 0.83 (39)

Ran a `/goal` full review + fix pass. Six parallel review agents across Swift concurrency,
security/privacy, correctness/data, SwiftUI UI/UX/accessibility, architecture/performance,
and tests/docs.

### Headline: ~14 reviewer-claimed critical/high findings, ALL refuted

Every claimed critical/high was adversarially verified against real source before any edit.
None survived. Notable false positives (kept for future reference):

- `BadgeService.deduplicateBadges` `max(by: { $0.earnedAt < $1.earnedAt })` is CORRECT — it
  returns the newest. The agent's proposed "fix" (`>`) would have *introduced* a bug. Classic
  case for verify-before-fix.
- `GoalService.goal(for:)` nil-`endDate` matching future dates is intended open-ended-goal
  semantics (only one open goal exists at a time).
- `HealthKitSyncService` `last7DaysAverage` is guarded by `.isEmpty ? 0`; `total / 7` is an
  intentional fixed 7-day rolling average over an Int field (already justified in 0.82 cycle).
- RevenueCat `.informational` verification mode is documented-intentional with manual
  fail-closed on `verification == .failed` at PremiumAccessStore.swift:145. Flipping to
  `.enforced` is a revenue-affecting product decision — NOT made autonomously.
- `Config/Local.xcconfig` "committed test key" — file is gitignored, 0 history entries; only
  `Local.xcconfig.example` is tracked.
- `forcedPremiumEnabled` is gated by `LaunchConfiguration.isOverridable` (Release: live
  XCTest/UITest harness only, uncheatable by launch args).
- `PremiumAccessStore.customerInfoTask` — non-detached `Task {}` in a `@MainActor` class
  inherits main isolation; no race.
- `InsightService` / `CoachService.send` / `WorkoutSessionController.updateMetrics`
  reentrancy — all guard correctly (no `await` between check-and-set; `CoachService` bumps a
  generation token synchronously before awaiting; `updateMetrics` writes state with no
  intervening await and the metrics loop is its sole serial caller).
- `GPXRouteImporter` ordering is correct (access → size check → read); agent misread it.
- `DESIGN_SYSTEM.md` / `FRONTEND_GUIDELINES.md` both exist; `APP_FLOW.md` tabs are accurate.

### Actual fixes (all LOW/MEDIUM) + regression tests

1. `GoalService.setGoal` — single `now` timestamp so closed.endDate == new.startDate (no
   sub-ms gap that fell back to default goal). Test: GoalServiceTests no-gap.
2. `HealthKitSyncService.calculateTrend` — zero prior week + current activity → "increasing"
   (was "stable"). Test: HealthKitSyncServiceTests zero-baseline trend.
3. `StreakCalculator` — `streakStartDate = nil` when count == 0 (was tomorrow). No consumer
   today; fixed for model honesty. Tests: empty → nil, active → first day.
4. `WorkoutsView.importRoute` — parse GPX off the main actor via `Task.detached`
   (ImportedRoute is Sendable). MEDIUM perf; ≤5 MiB cap, behavior-preserving.
5. DesignTokens cleanup — 3 exact-value token swaps (WorkoutLiveActivityWidget cornerRadius
   20→CornerRadius.xl, frame 24→IconSize.sm; HealthAccessHelpSheet 20→IconSize.xs). Zero
   visual change. Remaining widget chart/ring literals justified as local layout math.

### Verification

- Targeted: 28 tests / 3 suites passed (3 new tests by name).
- Full suite (serialized): 451 tests / 77 suites, 0 failures, TEST SUCCEEDED. No XCUITest
  flake this run.
- Version bump: project.yml first (0.82/38 → 0.83/39), then xcodegen generate +
  restore-entitlements; confirmed propagated to pbxproj. Docs synced.
- Enforcement greps reduced: frame 5→2, cornerRadius 1→0.

### Blocker (reported, not bypassed)

"Deploy to staging" = TestFlight/ASC, credential-gated (.p8/ASC key absent, AGENTS forbids
committing). Reported per goal constraint; not attempted.

## Follow-up: 0.84 (40) bump + iMarcus install

Per user release ritual: bumped 0.83→0.84 (39→40), synced version docs (README, publishing
playbook, agent docs, test_plan; CLAUDE.md/AGENTS.md carry no hardcoded app version), xcodegen
regen, committed (a690823) + pushed.

Device install on iMarcus (iPhone 17 Pro Max, UDID <IMARCUS_DEVICE_ID>) via
`DEVELOPMENT_TEAM=<LOCAL_DEVELOPMENT_TEAM_ID> Scripts/install-on-device.sh --device-name iMarcus --launch`:
- Build: OK. Install: **OK** — `devicectl device info apps` confirms `com.mneves.aipedometer`
  at `0.84 (40)` on device. Embedded watch app auto-delivers to the paired Apple Watch.
- Launch: initially failed (device locked, `FBSOpenApplicationErrorDomain error 7`). A bounded
  background launch-retry loop (10× every 15s) then **succeeded on attempt 6** once the device
  was unlocked — "Launched application with com.mneves.aipedometer". Install + launch milestone
  complete for 0.84 (40).
- LESSON: `install-on-device.sh --launch` exits 1 if the device is locked at launch time even
  though the install succeeded. Verify install independently via `devicectl device info apps`
  (bundle version) rather than trusting the script's exit code when a launch-lock is possible.

## Second `/goal` full review cycle → 0.85 (41)

Another `/goal` full review + fix loop on top of 0.84 (40).

### CRITICAL tooling lesson — stale sandbox snapshot
- The sandboxed `Bash`/`grep` view was **frozen on a session-start snapshot** (`0.82 (38)`,
  pre-0.83 source: e.g. it reported `HealthAccessHelpSheet:138` as `frame(width: 20)` and
  project.yml as `0.82/38`), while `Read` + non-sandboxed `Bash` saw the live `0.84 (40)` tree.
- Detected by reconciling `git show HEAD:project.yml` (0.84) vs sandboxed grep (0.82) + `md5`.
- FIX: use `dangerouslyDisableSandbox: true` for all file inspection this session; trust `Read`
  over sandboxed `grep`. Several reviewer findings were stale because of this (re-verified as
  already-fixed-in-0.83 instead of acted on).

### Review (5 dims, adversarial verify) → 0 critical, 0 high, 0 medium
- `Workflow` tool attempt failed (subagents 0 tokens); fell back to 5 parallel `Agent` calls.
  3 delivered (concurrency, security, state/data); UX/a11y + tests lost to a transient server
  rate limit even after resume — completed those two dimensions myself (static + suite).
- Findings #2 (streak start) and zero-week-trend baseline were ALREADY fixed in 0.83 — withdrawn.

### One fix (LOW consistency)
- `StepTrackingService.mergeCurrentDaySummaryIfNeeded`: `Calendar.current.isDate` →
  `calculator.didCrossMidnight`. The only spot bypassing the injected day-boundary calculator.
  Behavior-preserving; covered by existing weekly-summary tests.

### Verified-real-but-justified (NOT fixed) — the important one
- **Live pedometer updates bypass `refreshChain`.** `MotionService` delivers ticks via
  `Task { @MainActor }`, so `updateLiveData` can interleave with an in-flight refresh on shared
  `todaySteps`/`liveBaseline` state. LOW + self-healing (refresh's synchronous tail re-seeds).
  The "obvious" fix (chain live updates onto refreshChain) is **net-negative**: breaks the
  synchronous live-update contract (~6 tests assert todaySteps right after simulateLiveUpdate)
  + adds a `Task` hop to every pedometer tick + edits the documented landmine code. Recorded in
  MEMORY.md and implementation-notes (`#finding-085-live-interleave`) so nobody "fixes" it blind.
- Also justified: HR `.strictEndDate` midnight (display-only), trend Int truncation (<0.3%, AI
  prose), GPX previewPoints limit==1 (unreachable), widget/watch geometry literals, RevenueCat
  `.informational`, gitignored non-secret RC key.

### Verification
- Security: **0** `URLSession`/network matches in app target → on-device-AI is structural, not
  just documented.
- `xcodebuild test -only-testing:AIPedometerTests` (iPhone 17) → 451 tests / 77 suites passed,
  TEST SUCCEEDED. Baseline + final build → BUILD SUCCEEDED, 0 code warnings.
- Bump: project.yml first (0.84/40 → 0.85/41) → xcodegen → restore-entitlements; verified in
  pbxproj. Docs synced (README, playbook, agent docs, test_plan; CHANGELOG [0.85]).
- Corrected MEMORY drift: the serialization test is in `HealthKitServiceTests.swift`, NOT a
  `StepTrackingServiceTests.swift` (no such file).
- Staging = TestFlight/ASC = credential-gated → reported, not attempted.
