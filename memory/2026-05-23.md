# 2026-05-23

## Architecture cleanup publish/install closeout
- User asked to take note, update all docs, commit, push, and install on `iMarcus`.
- Scoped publish set: GPX route import orchestration moved to `GPXRouteImporter`; active training-plan workout recommendation projection moved to `TrainingPlanRecord`; Workouts consumes both boundaries.
- Docs synced: `CHANGELOG.md`, `FOR_YOU_KNOW.md`, `MEMORY.md`, and `docs/agents/project-structure.md`.
- Verification before publish: full iPhone 17 simulator suite passed with 16 XCTest unit tests, 435 Swift Testing tests, and 15 UI tests; `git diff --check`, `Scripts/check-agents-sync.sh`, and `Scripts/verify-device-identifiers.sh` passed.
- Published commit `62868a8` to `origin/master`.
- Physical device install on `iMarcus` passed with `DEVELOPMENT_TEAM=<LOCAL_DEVELOPMENT_TEAM_ID> bash Scripts/install-on-device.sh --device-name "iMarcus" --launch`; initial launch attempts were denied while the device was locked, then the built-in retry launched `com.mneves.aipedometer` and reported install OK.
- Independent device verification passed with `xcrun devicectl device info apps --device "iMarcus" --bundle-id com.mneves.aipedometer`: `AI Pedometer` version `0.77`, bundle version `33`.
- Pre-existing dirty file left unstaged intentionally: `memory/2026-05-20.md`.

## 0.78 release bump publish/install closeout
- User asked to bump the app version, take note, update all docs, commit, push, and install on `iMarcus`.
- Assumption: next release metadata is `0.78 (34)`, because current source of truth was `0.77 (33)` and no alternate target version was provided.
- Scoped docs sync includes `README.md`, `CHANGELOG.md`, `docs/agents/build-and-dev.md`, `docs/agents/testing.md`, and `docs/appstore/PUBLISHING_PLAYBOOK.md`.
- Regenerated the local ignored Xcode project with `xcodegen generate`; `asc xcode version view --project AIPedometer.xcodeproj --target AIPedometer` confirmed version `0.78`, build `34`.
- Pre-publish checks passed: `git diff --check`, `bash Scripts/check-agents-sync.sh`, and `bash Scripts/verify-device-identifiers.sh`.
- Published release metadata commit `04606c3` to `origin/master`.
- Physical device install on `iMarcus` passed with `DEVELOPMENT_TEAM=<LOCAL_DEVELOPMENT_TEAM_ID> bash Scripts/install-on-device.sh --device-name "iMarcus" --launch`; Debug device build succeeded, the app installed, and `com.mneves.aipedometer` launched.
- Independent device verification passed with `xcrun devicectl device info apps --device "iMarcus" --bundle-id com.mneves.aipedometer`: `AI Pedometer` version `0.78`, bundle version `34`.
- Pre-existing dirty file still left unstaged intentionally: `memory/2026-05-20.md`.
