# 2026-07-20

## 20:33 -03 — CLAUDE.md / AGENTS.md instruction-dump reconciliation

User re-sent their global instruction set ("ADD TO CLAUDE.md / AGENTS.md"). Diffed every requested directive against the current files instead of blind-appending: most items (AST-Grep default + blocking hook, codex dispatch, agent-browser QA, reproducer-first bugs, autoreview, /admin + brute-force rules, Figure It Out, memory system, soft-delete, agent_planning rule, Xcode 26/27 doc paths) were already integrated by earlier sessions.

Genuine deltas added:
- "List unresolved questions at the end" -> Purpose sections of CLAUDE.md + AGENTS.md.
- ExecPlan for complex features/refactors -> both files, referencing `~/dev/GUIDELINES-REF/EXECPLANS-GUIDELINES.md` (repo has no `/PLANS.md`; user template pointed there), kept under `agent_planning/`.
- Destructive-command/production safety guard -> Security sections of both files (adapted; repo has no prod DB/VPS).
- Atomic-commit one-liners (path-scoped `git commit -- <paths>`, `git restore --staged :/ && git add ...` for new files) -> canonical home `docs/agents/git-workflow.md` + new compact "Git Commits"/"Commits" sections in CLAUDE.md/AGENTS.md.
- Model-routing mechanics (gpt-5.6 only via Codex CLI; Claude models via Agent/Workflow model param; thin-wrapper trick for terra in workflows) -> AGENTS.md Execution Routing, plus the FULL user-supplied model-defaults table + application rules (defaults not limits, taste >= 7 for user-facing, reviews via fable-5/opus-4.8, never Haiku) — user approved the table via "fix all" after initially being offered the compact-only option. CLAUDE.md carries a compact pointer instead of the table (it is the compact checklist by design).

Deliberate deviations reported to user: no PROJECT_STATUS.md/QUICKSTART.md created (repo forbids speculative markdown; CHANGELOG + MEMORY cover status); "walk me through your thought process step by step" not added verbatim (conflicts with concision rule; existing challenge-assumptions bullets carry intent).

Pre-existing drift found and fixed: `check-agents-sync.sh` failed on arrival because upstream `~/dev/GUIDELINES-REF/AGENTS.md` gained the 2026-07-19 run-note TODO line (release 2026.07.19.0). Added the single missing line to the synced GUIDELINES-REF section; gate now green. All local edits stayed above `## GUIDELINES-REF` (script diffs only that section against upstream).

Files touched: CLAUDE.md, AGENTS.md, docs/agents/git-workflow.md, memory/2026-07-20.md. Verified with `bash Scripts/check-agents-sync.sh` (pass, before and after the table addition). Docs-only change; no browser surface, no test impact. Committed per user "fix all" instruction; commit hash appended below.
