# 2026-07-21

## Overnight pipeline (continued from 2026-07-20 session)

### Verified results

- **E2E green**: `Scripts/e2e-simulator.sh` full pass — 592/592 unit, 16/16 XCUITest, watchOS build, screenshots. EXIT:0.
- **TOOLCHAIN PINCER RESOLVED**: full Release `archive` succeeded with stable Xcode 26.6 + embedded watch
  (`output/archives/AIPedometer-095-gate.xcarchive`, signed TeamIdentifier Q96FUTC5G8). The 2026-06-13
  actool/watchOS-runtime pincer is dead — restoring watchOS 26.5 (23T570) via
  `xcodebuild -downloadPlatform watchOS` fixed it. Staging/prod are build-side unblocked.
- **Security review**: diff-scoped agent + pre-prod sweep agent — ZERO findings above threshold.
  Verified clean: secrets (Local.xcconfig gitignored, 0 history), GPX (XXE-safe, size caps),
  WatchConnectivity (strict Codable, same-team pairing), AI link policy, Release launch-arg lockdown,
  entitlements minimal-by-default, purchase fail-closed at every ingestion point, CI pinned.
  2 hygiene notes (below bar, NOT fixed): AppConstants.resolveAppStoreID ungated env read;
  GPX route name up to 1M chars persisted.
- **Autoreview**: 1 confirmed finding (withObservationTracking re-arm could accumulate registrations
  → disarm-before-rearm flag), 6 refuted. 23/23 affected tests green.

### Improve deep audit — vet pass (repo lesson held: verify every agent claim)

- 4/6 Claude audit agents landed (tests, perf, deps, DX/docs/direction); 2 died on subagent quota
  (correctness + tech-debt re-dispatched to **codex exec**, rollout healthy).
- **TEST-01 REFUTED in vet**: agent claimed StepTrackingService "never instantiated" in tests —
  FALSE; it's instantiated directly in HealthKitServiceTests.swift (lines 363/389/639/671/1589+).
  MEMORY.md already said the suite is misnamed, not missing. Residual: suite/file naming + gap analysis.
- Confirmed in source: PERF-01 (LA update per 5s tick), PERF-02 (serial awaits: distance/floors/heartRate
  mutually independent), PERF-03 (HistoryView hardcodes forceRefresh: true on load), PERF-07
  (unbounded pending-export fetches), DEPS-01 (CoachService mapError plain default vs @unknown default),
  DEPS-02 (SchemaV1 borrows 7 live classes), DEPS-03/04/07, DX-02 (CI pins Xcode_26.3.app),
  TEST-02 (no finish-workout XCUITest journey), TEST-04 (WatchSyncService hardcodes UserDefaults.standard).
- Direction options landed: DIR-01 PCC fallback (contradicts PRD local-first — user decision),
  DIR-02 watch as workout remote, DIR-03 GPX route-follow, DIR-04 finish 1.0 launch blockers.

### Deploy gates still open (user decisions)

- RevenueCat key in Config/Local.xcconfig is Test Store (`test_…`) → Release ships premium fail-closed.
  Real `appl_…` key or accept fail-closed staging = REQUIRED decision before any upload.
- Flicker fix still blocked on user's screen recording (no speculative UI change).
